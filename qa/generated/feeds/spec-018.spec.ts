/**
 * Auto-generated Playwright test for feeds/spec-018
 * Source: e2e/specs/feeds/spec-018.md
 * Generated: 2026-03-14T13:54:54.911Z
 *
 * Each test case corresponds to one checkbox in the spec file.
 * The controller (qa/controller.ts) uses Playwright JSON output
 * and artifact existence to determine verdicts.
 *
 * DO NOT EDIT — regenerate with: npx tsx qa/spec-to-playwright.ts feeds spec-018
 */

import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';












import { assertFeedsCheckpoint } from '../../module-assertions/feeds';







test.describe('feeds / spec-018', () => {
  test.beforeEach(async ({ page }) => {
    // Dev mode auth bypass — no Clerk needed
    const baseUrl = process.env.BASE_URL ?? 'http://127.0.0.1:3001';
    const url = new URL(baseUrl);
    await page.context().addCookies([{
      name: '__playwright',
      value: 'true',
      domain: url.hostname,
      path: '/',
    }]);
  });

  test('cp-000: Journal empty state rounded-xl border border-dashed border-border-subtle px-4 py', async ({ page }) => {
    // Checkpoint 0: Journal empty state: `rounded-xl border border-dashed border-border-subtle px-4 py-6 text-center`
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Journal empty state: `rounded-xl border border-dashed border-border-subtle px-4 py-6 text-center`",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-000.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-000 Journal empty state: `rounded-xl border border-dashed border-border-subtle px-4 py-6 text-center`');
    }


    // This test validates: Journal empty state: `rounded-xl border border-dashed border-border-subtle px-4 py-6 text-center`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-001: Subscribe checks both journalisSubscribed property AND URL match against current', async ({ page }) => {
    // Checkpoint 1: Subscribe checks both `journal.isSubscribed` property AND URL match against current subscriptions
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Subscribe checks both `journal.isSubscribed` property AND URL match against current subscriptions",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-001.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-001 Subscribe checks both `journal.isSubscribed` property AND URL match against current subscriptions');
    }


    // This test validates: Subscribe checks both `journal.isSubscribed` property AND URL match against current subscriptions
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-002: subscribedUrls computed via useMemo from subscriptions array', async ({ page }) => {
    // Checkpoint 2: `subscribedUrls` computed via `useMemo` from subscriptions array
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`subscribedUrls` computed via `useMemo` from subscriptions array",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-002.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-002 `subscribedUrls` computed via `useMemo` from subscriptions array');
    }


    // This test validates: `subscribedUrls` computed via `useMemo` from subscriptions array
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-003: Suggested feeds hidden when search is active hasSearch datasuggestedFeeds', async ({ page }) => {
    // Checkpoint 3: Suggested feeds hidden when search is active: `hasSearch ? [] : data?.suggestedFeeds ?? []`
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Suggested feeds hidden when search is active: `hasSearch ? [] : data?.suggestedFeeds ?? []`",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-003.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-003 Suggested feeds hidden when search is active: `hasSearch ? [] : data?.suggestedFeeds ?? []`');
    }


    // This test validates: Suggested feeds hidden when search is active: `hasSearch ? [] : data?.suggestedFeeds ?? []`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-004: Browse feeds exclude suggested in browse mode feedsfilterfeed feedisSuggested', async ({ page }) => {
    // Checkpoint 4: Browse feeds exclude suggested in browse mode: `feeds.filter((feed) => !feed.isSuggested)`
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Browse feeds exclude suggested in browse mode: `feeds.filter((feed) => !feed.isSuggested)`",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-004.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-004 Browse feeds exclude suggested in browse mode: `feeds.filter((feed) => !feed.isSuggested)`');
    }


    // This test validates: Browse feeds exclude suggested in browse mode: `feeds.filter((feed) => !feed.isSuggested)`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-005: PubMed create feed uses trimmedSearch not raw search as query', async ({ page }) => {
    // Checkpoint 5: PubMed create feed uses `trimmedSearch` (not raw `search`) as query
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "PubMed create feed uses `trimmedSearch` (not raw `search`) as query",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-005.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-005 PubMed create feed uses `trimmedSearch` (not raw `search`) as query');
    }


    // This test validates: PubMed create feed uses `trimmedSearch` (not raw `search`) as query
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-006: PubMed success message green text text-xs text-green-500', async ({ page }) => {
    // Checkpoint 6: PubMed success message: green text `text-xs text-green-500`
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "PubMed success message: green text `text-xs text-green-500`",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-006.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-006 PubMed success message: green text `text-xs text-green-500`');
    }


    // This test validates: PubMed success message: green text `text-xs text-green-500`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-007: PubMed error message red text text-xs text-red-400', async ({ page }) => {
    // Checkpoint 7: PubMed error message: red text `text-xs text-red-400`
    // Section: Quick Test Workflows > Journal Browser (journal-browser.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "PubMed error message: red text `text-xs text-red-400`",
      section: "Quick Test Workflows",
      subsection: "Journal Browser (journal-browser.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-007.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-007 PubMed error message: red text `text-xs text-red-400`');
    }


    // This test validates: PubMed error message: red text `text-xs text-red-400`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-008: showAdvancedFilters state boolean default false', async ({ page }) => {
    // Checkpoint 8: `showAdvancedFilters` state: boolean, default `false`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`showAdvancedFilters` state: boolean, default `false`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-008.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-008 `showAdvancedFilters` state: boolean, default `false`');
    }


    // This test validates: `showAdvancedFilters` state: boolean, default `false`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-009: loadSubscriptions clears error at start isLoadingSubscriptions true error null', async ({ page }) => {
    // Checkpoint 9: `loadSubscriptions` clears error at start: `{ isLoadingSubscriptions: true, error: null }`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadSubscriptions` clears error at start: `{ isLoadingSubscriptions: true, error: null }`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-009.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-009 `loadSubscriptions` clears error at start: `{ isLoadingSubscriptions: true, error: null }`');
    }


    // This test validates: `loadSubscriptions` clears error at start: `{ isLoadingSubscriptions: true, error: null }`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-010: loadSubscriptions fetches from GET apifeeds endpoint', async ({ page }) => {
    // Checkpoint 10: `loadSubscriptions` fetches from `GET /api/feeds` endpoint
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadSubscriptions` fetches from `GET /api/feeds` endpoint",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-010.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-010 `loadSubscriptions` fetches from `GET /api/feeds` endpoint');
    }


    // This test validates: `loadSubscriptions` fetches from `GET /api/feeds` endpoint
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-011: loadSubscriptions error message from Error instance or fallback Failed to load s', async ({ page }) => {
    // Checkpoint 11: `loadSubscriptions` error message from `Error` instance or fallback "Failed to load subscriptions"
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadSubscriptions` error message from `Error` instance or fallback \"Failed to load subscriptions\"",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-011.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-011 `loadSubscriptions` error message from `Error` instance or fallback "Failed to load subscriptions"');
    }


    // This test validates: `loadSubscriptions` error message from `Error` instance or fallback "Failed to load subscriptions"
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-012: loadArticles always sends sortDir parameter even for default desc', async ({ page }) => {
    // Checkpoint 12: `loadArticles` always sends `sortDir` parameter (even for default "desc")
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadArticles` always sends `sortDir` parameter (even for default \"desc\")",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-012.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-012 `loadArticles` always sends `sortDir` parameter (even for default "desc")');
    }


    // This test validates: `loadArticles` always sends `sortDir` parameter (even for default "desc")
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-013: loadArticles only sends sortBy when NOT newest omits for default to reduce param', async ({ page }) => {
    // Checkpoint 13: `loadArticles` only sends `sortBy` when NOT "newest" (omits for default to reduce params)
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadArticles` only sends `sortBy` when NOT \"newest\" (omits for default to reduce params)",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-013.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-013 `loadArticles` only sends `sortBy` when NOT "newest" (omits for default to reduce params)');
    }


    // This test validates: `loadArticles` only sends `sortBy` when NOT "newest" (omits for default to reduce params)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-014: loadArticles sends isReadfalse for unread filter isStarredtrue for starred filte', async ({ page }) => {
    // Checkpoint 14: `loadArticles` sends `isRead=false` for unread filter, `isStarred=true` for starred filter
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadArticles` sends `isRead=false` for unread filter, `isStarred=true` for starred filter",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-014.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-014 `loadArticles` sends `isRead=false` for unread filter, `isStarred=true` for starred filter');
    }


    // This test validates: `loadArticles` sends `isRead=false` for unread filter, `isStarred=true` for starred filter
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-015: subscribe POSTs to apifeeds with feedUrl body', async ({ page }) => {
    // Checkpoint 15: `subscribe` POSTs to `/api/feeds` with `{ feedUrl }` body
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`subscribe` POSTs to `/api/feeds` with `{ feedUrl }` body",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-015.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-015 `subscribe` POSTs to `/api/feeds` with `{ feedUrl }` body');
    }


    // This test validates: `subscribe` POSTs to `/api/feeds` with `{ feedUrl }` body
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-016: subscribe re-throws error after setting store error enables caller catch', async ({ page }) => {
    // Checkpoint 16: `subscribe` re-throws error after setting store error (enables caller catch)
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`subscribe` re-throws error after setting store error (enables caller catch)",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-016.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-016 `subscribe` re-throws error after setting store error (enables caller catch)');
    }


    // This test validates: `subscribe` re-throws error after setting store error (enables caller catch)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-017: subscribePubMed POSTs to apifeeds with pubmedQuery query body', async ({ page }) => {
    // Checkpoint 17: `subscribePubMed` POSTs to `/api/feeds` with `{ pubmedQuery: query }` body
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`subscribePubMed` POSTs to `/api/feeds` with `{ pubmedQuery: query }` body",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-017.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-017 `subscribePubMed` POSTs to `/api/feeds` with `{ pubmedQuery: query }` body');
    }


    // This test validates: `subscribePubMed` POSTs to `/api/feeds` with `{ pubmedQuery: query }` body
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-018: subscribePubMed re-throws error after setting store error', async ({ page }) => {
    // Checkpoint 18: `subscribePubMed` re-throws error after setting store error
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`subscribePubMed` re-throws error after setting store error",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-018.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-018 `subscribePubMed` re-throws error after setting store error');
    }


    // This test validates: `subscribePubMed` re-throws error after setting store error
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-019: markRead is fire-and-forget synchronous optimistic update fetchcatchrevert', async ({ page }) => {
    // Checkpoint 19: `markRead` is fire-and-forget: synchronous optimistic update + `fetch().catch(revert)`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`markRead` is fire-and-forget: synchronous optimistic update + `fetch().catch(revert)`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-019.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-019 `markRead` is fire-and-forget: synchronous optimistic update + `fetch().catch(revert)`');
    }


    // This test validates: `markRead` is fire-and-forget: synchronous optimistic update + `fetch().catch(revert)`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-020: markRead skips if article already read if article articleisRead return', async ({ page }) => {
    // Checkpoint 20: `markRead` skips if article already read (`if (!article || article.isRead) return`)
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`markRead` skips if article already read (`if (!article || article.isRead) return`)",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-020.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-020 `markRead` skips if article already read (`if (!article || article.isRead) return`)');
    }


    // This test validates: `markRead` skips if article already read (`if (!article || article.isRead) return`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-021: markRead POSTs to apifeedsarticlesarticleIdread', async ({ page }) => {
    // Checkpoint 21: `markRead` POSTs to `/api/feeds/articles/${articleId}/read`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`markRead` POSTs to `/api/feeds/articles/${articleId}/read`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-021.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-021 `markRead` POSTs to `/api/feeds/articles/${articleId}/read`');
    }


    // This test validates: `markRead` POSTs to `/api/feeds/articles/${articleId}/read`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-022: toggleStar POSTs to apifeedsarticlesarticleIdstar', async ({ page }) => {
    // Checkpoint 22: `toggleStar` POSTs to `/api/feeds/articles/${articleId}/star`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`toggleStar` POSTs to `/api/feeds/articles/${articleId}/star`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-022.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-022 `toggleStar` POSTs to `/api/feeds/articles/${articleId}/star`');
    }


    // This test validates: `toggleStar` POSTs to `/api/feeds/articles/${articleId}/star`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-023: saveToLibrary POSTs to apifeedsarticlesarticleIdsave', async ({ page }) => {
    // Checkpoint 23: `saveToLibrary` POSTs to `/api/feeds/articles/${articleId}/save`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`saveToLibrary` POSTs to `/api/feeds/articles/${articleId}/save`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-023.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-023 `saveToLibrary` POSTs to `/api/feeds/articles/${articleId}/save`');
    }


    // This test validates: `saveToLibrary` POSTs to `/api/feeds/articles/${articleId}/save`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-024: markAllRead POSTs to apifeedsarticlesmark-all-read with optional feedSourceId', async ({ page }) => {
    // Checkpoint 24: `markAllRead` POSTs to `/api/feeds/articles/mark-all-read` with optional `{ feedSourceId }`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`markAllRead` POSTs to `/api/feeds/articles/mark-all-read` with optional `{ feedSourceId }`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-024.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-024 `markAllRead` POSTs to `/api/feeds/articles/mark-all-read` with optional `{ feedSourceId }`');
    }


    // This test validates: `markAllRead` POSTs to `/api/feeds/articles/mark-all-read` with optional `{ feedSourceId }`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-025: markAllRead on success calls loadSubscriptions to refresh unread counts', async ({ page }) => {
    // Checkpoint 25: `markAllRead` on success calls `loadSubscriptions()` to refresh unread counts
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`markAllRead` on success calls `loadSubscriptions()` to refresh unread counts",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-025.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-025 `markAllRead` on success calls `loadSubscriptions()` to refresh unread counts');
    }


    // This test validates: `markAllRead` on success calls `loadSubscriptions()` to refresh unread counts
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-026: loadArticleNote GETs apifeedsarticlesarticleIdnotes', async ({ page }) => {
    // Checkpoint 26: `loadArticleNote` GETs `/api/feeds/articles/${articleId}/notes`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadArticleNote` GETs `/api/feeds/articles/${articleId}/notes`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-026.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-026 `loadArticleNote` GETs `/api/feeds/articles/${articleId}/notes`');
    }


    // This test validates: `loadArticleNote` GETs `/api/feeds/articles/${articleId}/notes`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-027: loadArticleNote silently returns on non-ok response no error set', async ({ page }) => {
    // Checkpoint 27: `loadArticleNote` silently returns on non-ok response (no error set)
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadArticleNote` silently returns on non-ok response (no error set)",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-027.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-027 `loadArticleNote` silently returns on non-ok response (no error set)');
    }


    // This test validates: `loadArticleNote` silently returns on non-ok response (no error set)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-028: loadArticleNote stores empty notes by deleting key from articleNotes map', async ({ page }) => {
    // Checkpoint 28: `loadArticleNote` stores empty notes by deleting key from `articleNotes` map
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadArticleNote` stores empty notes by deleting key from `articleNotes` map",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-028.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-028 `loadArticleNote` stores empty notes by deleting key from `articleNotes` map');
    }


    // This test validates: `loadArticleNote` stores empty notes by deleting key from `articleNotes` map
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-029: saveArticleNote trims notes before storingsending', async ({ page }) => {
    // Checkpoint 29: `saveArticleNote` trims notes before storing/sending
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`saveArticleNote` trims notes before storing/sending",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-029.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-029 `saveArticleNote` trims notes before storing/sending');
    }


    // This test validates: `saveArticleNote` trims notes before storing/sending
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-030: saveArticleNote sends null to API for empty notes notes trimmedNotes null', async ({ page }) => {
    // Checkpoint 30: `saveArticleNote` sends `null` to API for empty notes: `{ notes: trimmedNotes || null }`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`saveArticleNote` sends `null` to API for empty notes: `{ notes: trimmedNotes || null }`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-030.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-030 `saveArticleNote` sends `null` to API for empty notes: `{ notes: trimmedNotes || null }`');
    }


    // This test validates: `saveArticleNote` sends `null` to API for empty notes: `{ notes: trimmedNotes || null }`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-031: saveArticleNote failure is silent catch block empty', async ({ page }) => {
    // Checkpoint 31: `saveArticleNote` failure is silent (catch block empty)
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`saveArticleNote` failure is silent (catch block empty)",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-031.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-031 `saveArticleNote` failure is silent (catch block empty)');
    }


    // This test validates: `saveArticleNote` failure is silent (catch block empty)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-032: setLayout does NOT trigger article reload only updates layout state', async ({ page }) => {
    // Checkpoint 32: `setLayout` does NOT trigger article reload (only updates layout state)
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`setLayout` does NOT trigger article reload (only updates layout state)",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-032.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-032 `setLayout` does NOT trigger article reload (only updates layout state)');
    }


    // This test validates: `setLayout` does NOT trigger article reload (only updates layout state)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-033: clearFilters does NOT reset searchQuery only date and journal', async ({ page }) => {
    // Checkpoint 33: `clearFilters` does NOT reset `searchQuery` (only date and journal)
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`clearFilters` does NOT reset `searchQuery` (only date and journal)",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-033.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-033 `clearFilters` does NOT reset `searchQuery` (only date and journal)');
    }


    // This test validates: `clearFilters` does NOT reset `searchQuery` (only date and journal)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-034: loadJournals fetches from GET apifeedsarticlesjournals', async ({ page }) => {
    // Checkpoint 34: `loadJournals` fetches from `GET /api/feeds/articles/journals`
    // Section: Quick Test Workflows > Feed Store — Additional Details (feed-store.ts)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-018');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`loadJournals` fetches from `GET /api/feeds/articles/journals`",
      section: "Quick Test Workflows",
      subsection: "Feed Store — Additional Details (feed-store.ts)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-034.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-034 `loadJournals` fetches from `GET /api/feeds/articles/journals`');
    }


    // This test validates: `loadJournals` fetches from `GET /api/feeds/articles/journals`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });
});
