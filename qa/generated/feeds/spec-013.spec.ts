/**
 * Auto-generated Playwright test for feeds/spec-013
 * Source: e2e/specs/feeds/spec-013.md
 * Generated: 2026-03-14T10:17:05.963Z
 *
 * Each test case corresponds to one checkbox in the spec file.
 * The controller (qa/controller.ts) uses Playwright JSON output
 * and artifact existence to determine verdicts.
 *
 * DO NOT EDIT — regenerate with: npx tsx qa/spec-to-playwright.ts feeds spec-013
 */

import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';












import { assertFeedsCheckpoint } from '../../module-assertions/feeds';







test.describe('feeds / spec-013', () => {
  test.beforeEach(async ({ page }) => {
    // Dev mode auth bypass — no Clerk needed
    const baseUrl = process.env.BASE_URL ?? 'http://127.0.0.1:3001';
    const url = new URL(baseUrl);
    await page.context().addCookies([{
      name: '__playwright',
      value: 'true',
      domain: url.hostname,
      path: '/',
    }]);
  });

  test('cp-000: List view does NOT render a favicon image', async ({ page }) => {
    // Checkpoint 0: List view does NOT render a favicon image
    // Section: Quick Test Workflows > Article Card — List View (article-card-list.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "List view does NOT render a favicon image",
      section: "Quick Test Workflows",
      subsection: "Article Card — List View (article-card-list.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-000.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-000 ' + "List view does NOT render a favicon image");
    }


    // This test validates: List view does NOT render a favicon image
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-001: List view does NOT render authors', async ({ page }) => {
    // Checkpoint 1: List view does NOT render authors
    // Section: Quick Test Workflows > Article Card — List View (article-card-list.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "List view does NOT render authors",
      section: "Quick Test Workflows",
      subsection: "Article Card — List View (article-card-list.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-001.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-001 ' + "List view does NOT render authors");
    }


    // This test validates: List view does NOT render authors
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-002: List view does NOT render abstractsnippet text', async ({ page }) => {
    // Checkpoint 2: List view does NOT render abstract/snippet text
    // Section: Quick Test Workflows > Article Card — List View (article-card-list.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "List view does NOT render abstract/snippet text",
      section: "Quick Test Workflows",
      subsection: "Article Card — List View (article-card-list.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-002.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-002 ' + "List view does NOT render abstract/snippet text");
    }


    // This test validates: List view does NOT render abstract/snippet text
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-003: Magazine card glass-panel rounded-2xl overflow-hidden', async ({ page }) => {
    // Checkpoint 3: Magazine card: `glass-panel rounded-2xl overflow-hidden`
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine card: `glass-panel rounded-2xl overflow-hidden`",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-003.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-003 ' + "Magazine card: `glass-panel rounded-2xl overflow-hidden`");
    }


    // This test validates: Magazine card: `glass-panel rounded-2xl overflow-hidden`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-004: Magazine hover state unselected hoverring-1 hoverring-border', async ({ page }) => {
    // Checkpoint 4: Magazine hover state (unselected): `hover:ring-1 hover:ring-border`
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine hover state (unselected): `hover:ring-1 hover:ring-border`",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-004.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-004 ' + "Magazine hover state (unselected): `hover:ring-1 hover:ring-border`");
    }


    // This test validates: Magazine hover state (unselected): `hover:ring-1 hover:ring-border`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-005: Image container w-full h-48 bg-surface-raised overflow-hidden bg visible when im', async ({ page }) => {
    // Checkpoint 5: Image container: `w-full h-48 bg-surface-raised overflow-hidden` (bg visible when image loading)
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Image container: `w-full h-48 bg-surface-raised overflow-hidden` (bg visible when image loading)",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-005.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-005 ' + "Image container: `w-full h-48 bg-surface-raised overflow-hidden` (bg visible when image loading)");
    }


    // This test validates: Image container: `w-full h-48 bg-surface-raised overflow-hidden` (bg visible when image loading)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-006: Image uses object-cover fit', async ({ page }) => {
    // Checkpoint 6: Image uses `object-cover` fit
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Image uses `object-cover` fit",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-006.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-006 ' + "Image uses `object-cover` fit");
    }


    // This test validates: Image uses `object-cover` fit
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-007: Content area padding p-5', async ({ page }) => {
    // Checkpoint 7: Content area padding: `p-5`
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Content area padding: `p-5`",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-007.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-007 ' + "Content area padding: `p-5`");
    }


    // This test validates: Content area padding: `p-5`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-008: Magazine title text-base larger than card views text-sm', async ({ page }) => {
    // Checkpoint 8: Magazine title: `text-base` (larger than card view's `text-sm`)
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine title: `text-base` (larger than card view's `text-sm`)",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-008.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-008 ' + "Magazine title: `text-base` (larger than card view's `text-sm`)");
    }


    // This test validates: Magazine title: `text-base` (larger than card view's `text-sm`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-009: Magazine read title font-medium text-ink-muted card view uses font-normal', async ({ page }) => {
    // Checkpoint 9: Magazine read title: `font-medium text-ink-muted` (card view uses `font-normal`)
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine read title: `font-medium text-ink-muted` (card view uses `font-normal`)",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-009.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-009 ' + "Magazine read title: `font-medium text-ink-muted` (card view uses `font-normal`)");
    }


    // This test validates: Magazine read title: `font-medium text-ink-muted` (card view uses `font-normal`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-010: Magazine abstract shows full articleabstractSnippet NOT truncated to 120 chars l', async ({ page }) => {
    // Checkpoint 10: Magazine abstract shows full `article.abstractSnippet` (NOT truncated to 120 chars like card view)
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine abstract shows full `article.abstractSnippet` (NOT truncated to 120 chars like card view)",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-010.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-010 ' + "Magazine abstract shows full `article.abstractSnippet` (NOT truncated to 120 chars like card view)");
    }


    // This test validates: Magazine abstract shows full `article.abstractSnippet` (NOT truncated to 120 chars like card view)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-011: Magazine abstract styling text-sm text-ink-muted line-clamp-3 leading-relaxed', async ({ page }) => {
    // Checkpoint 11: Magazine abstract styling: `text-sm text-ink-muted line-clamp-3 leading-relaxed`
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine abstract styling: `text-sm text-ink-muted line-clamp-3 leading-relaxed`",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-011.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-011 ' + "Magazine abstract styling: `text-sm text-ink-muted line-clamp-3 leading-relaxed`");
    }


    // This test validates: Magazine abstract styling: `text-sm text-ink-muted line-clamp-3 leading-relaxed`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-012: Magazine external link text Open NOT DOI like card view', async ({ page }) => {
    // Checkpoint 12: Magazine external link text: "Open" (NOT "DOI" like card view)
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine external link text: \"Open\" (NOT \"DOI\" like card view)",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-012.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-012 ' + "Magazine external link text: \"Open\" (NOT \"DOI\" like card view)");
    }


    // This test validates: Magazine external link text: "Open" (NOT "DOI" like card view)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-013: Magazine external link uses articlelink NOT httpsdoiorgarticledoi', async ({ page }) => {
    // Checkpoint 13: Magazine external link uses `article.link` (NOT `https://doi.org/${article.doi}`)
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine external link uses `article.link` (NOT `https://doi.org/${article.doi}`)",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-013.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-013 ' + "Magazine external link uses `article.link` (NOT `https://doi.org/${article.doi}`)");
    }


    // This test validates: Magazine external link uses `article.link` (NOT `https://doi.org/${article.doi}`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-014: Magazine external link only shows when articlelink is truthy NOT articledoi', async ({ page }) => {
    // Checkpoint 14: Magazine external link only shows when `article.link` is truthy (NOT `article.doi`)
    // Section: Quick Test Workflows > Article Card — Magazine View (article-card-magazine.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Magazine external link only shows when `article.link` is truthy (NOT `article.doi`)",
      section: "Quick Test Workflows",
      subsection: "Article Card — Magazine View (article-card-magazine.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-014.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-014 ' + "Magazine external link only shows when `article.link` is truthy (NOT `article.doi`)");
    }


    // This test validates: Magazine external link only shows when `article.link` is truthy (NOT `article.doi`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-015: Search input pl-9 pr-4 py-2 rounded-xl text-xs', async ({ page }) => {
    // Checkpoint 15: Search input: `pl-9 pr-4 py-2 rounded-xl text-xs`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Search input: `pl-9 pr-4 py-2 rounded-xl text-xs`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-015.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-015 ' + "Search input: `pl-9 pr-4 py-2 rounded-xl text-xs`");
    }


    // This test validates: Search input: `pl-9 pr-4 py-2 rounded-xl text-xs`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-016: Search clear button position right-3 top-12 -translate-y-12', async ({ page }) => {
    // Checkpoint 16: Search clear button position: `right-3 top-1/2 -translate-y-1/2`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Search clear button position: `right-3 top-1/2 -translate-y-1/2`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-016.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-016 ' + "Search clear button position: `right-3 top-1/2 -translate-y-1/2`");
    }


    // This test validates: Search clear button position: `right-3 top-1/2 -translate-y-1/2`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-017: Filter toggle with active filters bg-brand10 text-brand border-brand20', async ({ page }) => {
    // Checkpoint 17: Filter toggle with active filters: `bg-brand/10 text-brand border-brand/20`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Filter toggle with active filters: `bg-brand/10 text-brand border-brand/20`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-017.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-017 ' + "Filter toggle with active filters: `bg-brand/10 text-brand border-brand/20`");
    }


    // This test validates: Filter toggle with active filters: `bg-brand/10 text-brand border-brand/20`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-018: Filter toggle without active filters bg-surface-raised text-ink-muted border-bor', async ({ page }) => {
    // Checkpoint 18: Filter toggle without active filters: `bg-surface-raised text-ink-muted border-border hover:text-ink`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Filter toggle without active filters: `bg-surface-raised text-ink-muted border-border hover:text-ink`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-018.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-018 ' + "Filter toggle without active filters: `bg-surface-raised text-ink-muted border-border hover:text-ink`");
    }


    // This test validates: Filter toggle without active filters: `bg-surface-raised text-ink-muted border-border hover:text-ink`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-019: Blue dot indicator w-15 h-15 rounded-full bg-brand', async ({ page }) => {
    // Checkpoint 19: Blue dot indicator: `w-1.5 h-1.5 rounded-full bg-brand`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Blue dot indicator: `w-1.5 h-1.5 rounded-full bg-brand`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-019.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-019 ' + "Blue dot indicator: `w-1.5 h-1.5 rounded-full bg-brand`");
    }


    // This test validates: Blue dot indicator: `w-1.5 h-1.5 rounded-full bg-brand`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-020: Sort toggle icon SortAscending when sortBy oldest SortDescending otherwise', async ({ page }) => {
    // Checkpoint 20: Sort toggle icon: `SortAscending` when `sortBy === "oldest"`, `SortDescending` otherwise
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Sort toggle icon: `SortAscending` when `sortBy === \"oldest\"`, `SortDescending` otherwise",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-020.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-020 ' + "Sort toggle icon: `SortAscending` when `sortBy === \"oldest\"`, `SortDescending` otherwise");
    }


    // This test validates: Sort toggle icon: `SortAscending` when `sortBy === "oldest"`, `SortDescending` otherwise
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-021: Advanced filter panel glass-panel rounded-xl p-3 space-y-3 only rendered when sh', async ({ page }) => {
    // Checkpoint 21: Advanced filter panel: `glass-panel rounded-xl p-3 space-y-3` (only rendered when `showAdvancedFilters`)
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Advanced filter panel: `glass-panel rounded-xl p-3 space-y-3` (only rendered when `showAdvancedFilters`)",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-021.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-021 ' + "Advanced filter panel: `glass-panel rounded-xl p-3 space-y-3` (only rendered when `showAdvancedFilters`)");
    }


    // This test validates: Advanced filter panel: `glass-panel rounded-xl p-3 space-y-3` (only rendered when `showAdvancedFilters`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-022: Date filter From label text-10px text-ink-muted font-medium uppercase tracking-w', async ({ page }) => {
    // Checkpoint 22: Date filter "From" label: `text-[10px] text-ink-muted font-medium uppercase tracking-wide`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Date filter \"From\" label: `text-[10px] text-ink-muted font-medium uppercase tracking-wide`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-022.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-022 ' + "Date filter \"From\" label: `text-[10px] text-ink-muted font-medium uppercase tracking-wide`");
    }


    // This test validates: Date filter "From" label: `text-[10px] text-ink-muted font-medium uppercase tracking-wide`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-023: Date filter To label same styling as From', async ({ page }) => {
    // Checkpoint 23: Date filter "To" label: same styling as "From"
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Date filter \"To\" label: same styling as \"From\"",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-023.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-023 ' + "Date filter \"To\" label: same styling as \"From\"");
    }


    // This test validates: Date filter "To" label: same styling as "From"
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-024: Date inputs are typedate native HTML date picker', async ({ page }) => {
    // Checkpoint 24: Date inputs are `type="date"` (native HTML date picker)
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Date inputs are `type=\"date\"` (native HTML date picker)",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-024.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-024 ' + "Date inputs are `type=\"date\"` (native HTML date picker)");
    }


    // This test validates: Date inputs are `type="date"` (native HTML date picker)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-025: Journal dropdown label Journal uppercase tracking-wide', async ({ page }) => {
    // Checkpoint 25: Journal dropdown label: "Journal" (uppercase tracking-wide)
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Journal dropdown label: \"Journal\" (uppercase tracking-wide)",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-025.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-025 ' + "Journal dropdown label: \"Journal\" (uppercase tracking-wide)");
    }


    // This test validates: Journal dropdown label: "Journal" (uppercase tracking-wide)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-026: Journal dropdown default option text All journals', async ({ page }) => {
    // Checkpoint 26: Journal dropdown default option text: "All journals"
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Journal dropdown default option text: \"All journals\"",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-026.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-026 ' + "Journal dropdown default option text: \"All journals\"");
    }


    // This test validates: Journal dropdown default option text: "All journals"
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-027: Advanced sort labels Date for published Added Title 3 equal-width buttons', async ({ page }) => {
    // Checkpoint 27: Advanced sort labels: "Date" (for published), "Added", "Title" (3 equal-width buttons)
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Advanced sort labels: \"Date\" (for published), \"Added\", \"Title\" (3 equal-width buttons)",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-027.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-027 ' + "Advanced sort labels: \"Date\" (for published), \"Added\", \"Title\" (3 equal-width buttons)");
    }


    // This test validates: Advanced sort labels: "Date" (for published), "Added", "Title" (3 equal-width buttons)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-028: Advanced sort active bg-brand10 text-brand border border-brand20', async ({ page }) => {
    // Checkpoint 28: Advanced sort active: `bg-brand/10 text-brand border border-brand/20`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Advanced sort active: `bg-brand/10 text-brand border border-brand/20`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-028.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-028 ' + "Advanced sort active: `bg-brand/10 text-brand border border-brand/20`");
    }


    // This test validates: Advanced sort active: `bg-brand/10 text-brand border border-brand/20`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-029: Advanced sort inactive bg-surface-raised text-ink-muted border border-border hov', async ({ page }) => {
    // Checkpoint 29: Advanced sort inactive: `bg-surface-raised text-ink-muted border border-border hover:text-ink`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Advanced sort inactive: `bg-surface-raised text-ink-muted border border-border hover:text-ink`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-029.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-029 ' + "Advanced sort inactive: `bg-surface-raised text-ink-muted border border-border hover:text-ink`");
    }


    // This test validates: Advanced sort inactive: `bg-surface-raised text-ink-muted border border-border hover:text-ink`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-030: currentLegacySort derivation sortBy relevance title published maps store sort to', async ({ page }) => {
    // Checkpoint 30: `currentLegacySort` derivation: `sortBy === "relevance" ? "title" : "published"` — maps store sort to advanced sort visual state
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`currentLegacySort` derivation: `sortBy === \"relevance\" ? \"title\" : \"published\"` — maps store sort to advanced sort visual state",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-030.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-030 ' + "`currentLegacySort` derivation: `sortBy === \"relevance\" ? \"title\" : \"published\"` — maps store sort to advanced sort visual state");
    }


    // This test validates: `currentLegacySort` derivation: `sortBy === "relevance" ? "title" : "published"` — maps store sort to advanced sort visual state
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-031: Setting sort added with sortDir asc normalizes to store sortBy oldest', async ({ page }) => {
    // Checkpoint 31: Setting sort "added" with `sortDir = "asc"` normalizes to store `sortBy = "oldest"`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Setting sort \"added\" with `sortDir = \"asc\"` normalizes to store `sortBy = \"oldest\"`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-031.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-031 ' + "Setting sort \"added\" with `sortDir = \"asc\"` normalizes to store `sortBy = \"oldest\"`");
    }


    // This test validates: Setting sort "added" with `sortDir = "asc"` normalizes to store `sortBy = "oldest"`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-032: Setting sort published with sortDir desc normalizes to store sortBy newest', async ({ page }) => {
    // Checkpoint 32: Setting sort "published" with `sortDir = "desc"` normalizes to store `sortBy = "newest"`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Setting sort \"published\" with `sortDir = \"desc\"` normalizes to store `sortBy = \"newest\"`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-032.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-032 ' + "Setting sort \"published\" with `sortDir = \"desc\"` normalizes to store `sortBy = \"newest\"`");
    }


    // This test validates: Setting sort "published" with `sortDir = "desc"` normalizes to store `sortBy = "newest"`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-033: hasActiveFilters does NOT include searchQuery only checks filterDateFrom filterD', async ({ page }) => {
    // Checkpoint 33: `hasActiveFilters` does NOT include `searchQuery` — only checks `filterDateFrom || filterDateTo || filterJournal`
    // Section: Quick Test Workflows > Article Search Bar (article-search-bar.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "`hasActiveFilters` does NOT include `searchQuery` — only checks `filterDateFrom || filterDateTo || filterJournal`",
      section: "Quick Test Workflows",
      subsection: "Article Search Bar (article-search-bar.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-033.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-033 ' + "`hasActiveFilters` does NOT include `searchQuery` — only checks `filterDateFrom || filterDateTo || filterJournal`");
    }


    // This test validates: `hasActiveFilters` does NOT include `searchQuery` — only checks `filterDateFrom || filterDateTo || filterJournal`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-034: Reader panel glass-panel rounded-2xl h-full overflow-y-auto p-5', async ({ page }) => {
    // Checkpoint 34: Reader panel: `glass-panel rounded-2xl h-full overflow-y-auto p-5`
    // Section: Quick Test Workflows > Article Reader (article-reader.tsx)

    // Navigate to the page
    await page.goto('/feeds', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/feeds/spec-013');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertFeedsCheckpoint({
      page,
      description: "Reader panel: `glass-panel rounded-2xl h-full overflow-y-auto p-5`",
      section: "Quick Test Workflows",
      subsection: "Article Reader (article-reader.tsx)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-034.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled feeds checkpoint: cp-034 ' + "Reader panel: `glass-panel rounded-2xl h-full overflow-y-auto p-5`");
    }


    // This test validates: Reader panel: `glass-panel rounded-2xl h-full overflow-y-auto p-5`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });
});
