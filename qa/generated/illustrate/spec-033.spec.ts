/**
 * Auto-generated Playwright test for illustrate/spec-033
 * Source: e2e/specs/illustrate/spec-033.md
 * Generated: 2026-03-15T05:34:14.296Z
 *
 * Each test case corresponds to one checkbox in the spec file.
 * The controller (qa/controller.ts) uses Playwright JSON output
 * and artifact existence to determine verdicts.
 *
 * DO NOT EDIT — regenerate with: npx tsx qa/spec-to-playwright.ts illustrate spec-033
 */

import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';















import { assertIllustrateCheckpoint } from '../../module-assertions/illustrate';




test.describe('illustrate / spec-033', () => {
  test.beforeEach(async ({ page }) => {
    // Dev mode auth bypass — no Clerk needed
    const baseUrl = process.env.BASE_URL ?? 'http://127.0.0.1:3001';
    const url = new URL(baseUrl);
    await page.context().addCookies([{
      name: '__playwright',
      value: 'true',
      domain: url.hostname,
      path: '/',
    }]);
  });

  test('cp-000: No rate limiting applied', async ({ page }) => {
    // Checkpoint 0: No rate limiting applied
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "No rate limiting applied",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-000.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-000 ' + "No rate limiting applied");
    }


    // This test validates: No rate limiting applied
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-001: Input validation requires svg extension rejects names containing or', async ({ page }) => {
    // Checkpoint 1: Input validation: requires `.svg` extension, rejects names containing `..` or `/`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Input validation: requires `.svg` extension, rejects names containing `..` or `/`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-001.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-001 ' + "Input validation: requires `.svg` extension, rejects names containing `..` or `/`");
    }


    // This test validates: Input validation: requires `.svg` extension, rejects names containing `..` or `/`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-002: Error for missing name error Missing name parameter 400', async ({ page }) => {
    // Checkpoint 2: Error for missing name: `{ error: "Missing 'name' parameter" }` (400)
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Error for missing name: `{ error: \"Missing 'name' parameter\" }` (400)",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-002.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-002 ' + "Error for missing name: `{ error: \"Missing 'name' parameter\" }` (400)");
    }


    // This test validates: Error for missing name: `{ error: "Missing 'name' parameter" }` (400)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-003: Error for unsafe name error Invalid icon name 400', async ({ page }) => {
    // Checkpoint 3: Error for unsafe name: `{ error: "Invalid icon name" }` (400)
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Error for unsafe name: `{ error: \"Invalid icon name\" }` (400)",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-003.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-003 ' + "Error for unsafe name: `{ error: \"Invalid icon name\" }` (400)");
    }


    // This test validates: Error for unsafe name: `{ error: "Invalid icon name" }` (400)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-004: Response Content-Type imagesvgxml with Cache-Control public max-age31536000 immu', async ({ page }) => {
    // Checkpoint 4: Response `Content-Type: image/svg+xml` with `Cache-Control: public, max-age=31536000, immutable`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Response `Content-Type: image/svg+xml` with `Cache-Control: public, max-age=31536000, immutable`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-004.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-004 ' + "Response `Content-Type: image/svg+xml` with `Cache-Control: public, max-age=31536000, immutable`");
    }


    // This test validates: Response `Content-Type: image/svg+xml` with `Cache-Control: public, max-age=31536000, immutable`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-005: Production serves from R2 bucket at path iconsbioiconsname', async ({ page }) => {
    // Checkpoint 5: Production: serves from R2 bucket at path `icons/bioicons/{name}`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Production: serves from R2 bucket at path `icons/bioicons/{name}`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-005.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-005 ' + "Production: serves from R2 bucket at path `icons/bioicons/{name}`");
    }


    // This test validates: Production: serves from R2 bucket at path `icons/bioicons/{name}`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-006: Development serves from local dataiconsbioiconsname directory', async ({ page }) => {
    // Checkpoint 6: Development: serves from local `.data/icons/bioicons/{name}` directory
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Development: serves from local `.data/icons/bioicons/{name}` directory",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-006.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-006 ' + "Development: serves from local `.data/icons/bioicons/{name}` directory");
    }


    // This test validates: Development: serves from local `.data/icons/bioicons/{name}` directory
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-007: Dev 404 message Icon not found run upload script first production 404 Icon not f', async ({ page }) => {
    // Checkpoint 7: Dev 404 message: `Icon not found (run upload script first)`; production 404: `Icon not found`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Dev 404 message: `Icon not found (run upload script first)`; production 404: `Icon not found`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-007.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-007 ' + "Dev 404 message: `Icon not found (run upload script first)`; production 404: `Icon not found`");
    }


    // This test validates: Dev 404 message: `Icon not found (run upload script first)`; production 404: `Icon not found`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-008: No rate limiting applied unlike other illustration API routes', async ({ page }) => {
    // Checkpoint 8: No rate limiting applied (unlike other illustration API routes)
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/search` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "No rate limiting applied (unlike other illustration API routes)",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/search` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-008.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-008 ' + "No rate limiting applied (unlike other illustration API routes)");
    }


    // This test validates: No rate limiting applied (unlike other illustration API routes)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-009: Query parameter q is optional emptymissing returns empty results', async ({ page }) => {
    // Checkpoint 9: Query parameter `q` is optional; empty/missing returns empty results
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/search` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Query parameter `q` is optional; empty/missing returns empty results",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/search` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-009.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-009 ' + "Query parameter `q` is optional; empty/missing returns empty results");
    }


    // This test validates: Query parameter `q` is optional; empty/missing returns empty results
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-010: expand_synonyms parameter boolean default true', async ({ page }) => {
    // Checkpoint 10: `expand_synonyms` parameter: boolean, default `true`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/search` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`expand_synonyms` parameter: boolean, default `true`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/search` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-010.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-010 ' + "`expand_synonyms` parameter: boolean, default `true`");
    }


    // This test validates: `expand_synonyms` parameter: boolean, default `true`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-011: include_scores parameter boolean default false', async ({ page }) => {
    // Checkpoint 11: `include_scores` parameter: boolean, default `false`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/search` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`include_scores` parameter: boolean, default `false`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/search` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-011.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-011 ' + "`include_scores` parameter: boolean, default `false`");
    }


    // This test validates: `include_scores` parameter: boolean, default `false`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-012: Scoring algorithm 100 exact name match 50 starts-with 25 contains 20 category ma', async ({ page }) => {
    // Checkpoint 12: Scoring algorithm: `100` (exact name match), `50` (starts-with), `25` (contains), `20` (category match), `30`/`15`/`10` (keyword exact/starts/contains), `3` (synonym match)
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/search` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Scoring algorithm: `100` (exact name match), `50` (starts-with), `25` (contains), `20` (category match), `30`/`15`/`10` (keyword exact/starts/contains), `3` (synonym match)",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/search` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-012.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-012 ' + "Scoring algorithm: `100` (exact name match), `50` (starts-with), `25` (contains), `20` (category match), `30`/`15`/`10` (keyword exact/starts/contains), `3` (synonym match)");
    }


    // This test validates: Scoring algorithm: `100` (exact name match), `50` (starts-with), `25` (contains), `20` (category match), `30`/`15`/`10` (keyword exact/starts/contains), `3` (synonym match)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-013: Response includes expanded_terms array when expand_synonyms is true', async ({ page }) => {
    // Checkpoint 13: Response includes `expanded_terms` array when `expand_synonyms` is true
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/search` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Response includes `expanded_terms` array when `expand_synonyms` is true",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/search` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-013.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-013 ' + "Response includes `expanded_terms` array when `expand_synonyms` is true");
    }


    // This test validates: Response includes `expanded_terms` array when `expand_synonyms` is true
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-014: Category medical matches libraries health bioicons bioicons-full or categories a', async ({ page }) => {
    // Checkpoint 14: Category `medical` matches libraries `health`, `bioicons`, `bioicons-full`, or categories `anatomy`, `equipment`, `diagnostics`, `conditions`, `services`, and `biology`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/search` (GET)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Category `medical` matches libraries `health`, `bioicons`, `bioicons-full`, or categories `anatomy`, `equipment`, `diagnostics`, `conditions`, `services`, and `biology`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/search` (GET)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-014.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-014 ' + "Category `medical` matches libraries `health`, `bioicons`, `bioicons-full`, or categories `anatomy`, `equipment`, `diagnostics`, `conditions`, `services`, and `biology`");
    }


    // This test validates: Category `medical` matches libraries `health`, `bioicons`, `bioicons-full`, or categories `anatomy`, `equipment`, `diagnostics`, `conditions`, `services`, and `biology`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-015: Schema name 1100 chars style flatoutlinefilledminimal category scientificmedical', async ({ page }) => {
    // Checkpoint 15: Schema: `name` (1–100 chars), `style` (`flat`/`outline`/`filled`/`minimal`), `category` (`scientific`/`medical`/`ui`/`arrows`/`shapes`)
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/generate` (POST)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Schema: `name` (1–100 chars), `style` (`flat`/`outline`/`filled`/`minimal`), `category` (`scientific`/`medical`/`ui`/`arrows`/`shapes`)",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/generate` (POST)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-015.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-015 ' + "Schema: `name` (1–100 chars), `style` (`flat`/`outline`/`filled`/`minimal`), `category` (`scientific`/`medical`/`ui`/`arrows`/`shapes`)");
    }


    // This test validates: Schema: `name` (1–100 chars), `style` (`flat`/`outline`/`filled`/`minimal`), `category` (`scientific`/`medical`/`ui`/`arrows`/`shapes`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-016: color is optional size range 16512 default 64 useGemini boolean default false', async ({ page }) => {
    // Checkpoint 16: `color` is optional, `size` range 16–512 (default 64), `useGemini` boolean (default false)
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/generate` (POST)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`color` is optional, `size` range 16–512 (default 64), `useGemini` boolean (default false)",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/generate` (POST)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-016.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-016 ' + "`color` is optional, `size` range 16–512 (default 64), `useGemini` boolean (default false)");
    }


    // This test validates: `color` is optional, `size` range 16–512 (default 64), `useGemini` boolean (default false)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-017: Rate limited with RATE_LIMITSai', async ({ page }) => {
    // Checkpoint 17: Rate limited with `RATE_LIMITS.ai`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/generate` (POST)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Rate limited with `RATE_LIMITS.ai`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/generate` (POST)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-017.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-017 ' + "Rate limited with `RATE_LIMITS.ai`");
    }


    // This test validates: Rate limited with `RATE_LIMITS.ai`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-018: Response includes metadatapathCount and metadatapalette', async ({ page }) => {
    // Checkpoint 18: Response includes `metadata.pathCount` and `metadata.palette`
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/generate` (POST)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Response includes `metadata.pathCount` and `metadata.palette`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/generate` (POST)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-018.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-018 ' + "Response includes `metadata.pathCount` and `metadata.palette`");
    }


    // This test validates: Response includes `metadata.pathCount` and `metadata.palette`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-019: extractSVG strips markdown code block wrappers before returning SVG', async ({ page }) => {
    // Checkpoint 19: `extractSVG()` strips markdown code block wrappers before returning SVG
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/generate` (POST)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`extractSVG()` strips markdown code block wrappers before returning SVG",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/generate` (POST)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-019.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-019 ' + "`extractSVG()` strips markdown code block wrappers before returning SVG");
    }


    // This test validates: `extractSVG()` strips markdown code block wrappers before returning SVG
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-020: cleanSVG adds xmlns and viewBox attributes to malformed SVGs', async ({ page }) => {
    // Checkpoint 20: `cleanSVG()` adds `xmlns` and `viewBox` attributes to malformed SVGs
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/generate` (POST)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`cleanSVG()` adds `xmlns` and `viewBox` attributes to malformed SVGs",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/generate` (POST)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-020.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-020 ' + "`cleanSVG()` adds `xmlns` and `viewBox` attributes to malformed SVGs");
    }


    // This test validates: `cleanSVG()` adds `xmlns` and `viewBox` attributes to malformed SVGs
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-021: Gemini generation failure falls back to LLM-based SVG generation', async ({ page }) => {
    // Checkpoint 21: Gemini generation failure falls back to LLM-based SVG generation
    // Section: Quick Test Workflows > API Route: `/api/illustration/icons/generate` (POST)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Gemini generation failure falls back to LLM-based SVG generation",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/icons/generate` (POST)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-021.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-021 ' + "Gemini generation failure falls back to LLM-based SVG generation");
    }


    // This test validates: Gemini generation failure falls back to LLM-based SVG generation
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-022: Schema includes optional fields description svgContent canvasJson any mermaidSyn', async ({ page }) => {
    // Checkpoint 22: Schema includes optional fields: `description`, `svgContent`, `canvasJson` (any), `mermaidSyntax`, `domain`, `sourceBackend`, `sourcePrompt`, `width` (number), `height` (number)
    // Section: Quick Test Workflows > API Route: `/api/illustration/save` (POST) — Full Schema

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Schema includes optional fields: `description`, `svgContent`, `canvasJson` (any), `mermaidSyntax`, `domain`, `sourceBackend`, `sourcePrompt`, `width` (number), `height` (number)",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/save` (POST) — Full Schema",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-022.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-022 ' + "Schema includes optional fields: `description`, `svgContent`, `canvasJson` (any), `mermaidSyntax`, `domain`, `sourceBackend`, `sourcePrompt`, `width` (number), `height` (number)");
    }


    // This test validates: Schema includes optional fields: `description`, `svgContent`, `canvasJson` (any), `mermaidSyntax`, `domain`, `sourceBackend`, `sourcePrompt`, `width` (number), `height` (number)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-023: Mock success response includes userId field alongside echoed illustration data', async ({ page }) => {
    // Checkpoint 23: Mock success response includes `userId` field alongside echoed illustration data
    // Section: Quick Test Workflows > API Route: `/api/illustration/save` (POST) — Full Schema

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Mock success response includes `userId` field alongside echoed illustration data",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/save` (POST) — Full Schema",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-023.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-023 ' + "Mock success response includes `userId` field alongside echoed illustration data");
    }


    // This test validates: Mock success response includes `userId` field alongside echoed illustration data
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-024: traceGeneration called for observability tracing with tier modelId feature userI', async ({ page }) => {
    // Checkpoint 24: `traceGeneration()` called for observability tracing with `tier`, `modelId`, `feature`, `userId`
    // Section: Quick Test Workflows > API Route: `/api/illustration/generate` (POST) — Additional Details

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`traceGeneration()` called for observability tracing with `tier`, `modelId`, `feature`, `userId`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/generate` (POST) — Additional Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-024.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-024 ' + "`traceGeneration()` called for observability tracing with `tier`, `modelId`, `feature`, `userId`");
    }


    // This test validates: `traceGeneration()` called for observability tracing with `tier`, `modelId`, `feature`, `userId`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-025: Gemini vectorization colorCount is 16 for flat 32 for detailed and 16 again for ', async ({ page }) => {
    // Checkpoint 25: Gemini vectorization `colorCount` is `16` for `flat`, `32` for `detailed`, and `16` again for `schematic` or `photorealistic`
    // Section: Quick Test Workflows > API Route: `/api/illustration/generate` (POST) — Additional Details

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Gemini vectorization `colorCount` is `16` for `flat`, `32` for `detailed`, and `16` again for `schematic` or `photorealistic`",
      section: "Quick Test Workflows",
      subsection: "API Route: `/api/illustration/generate` (POST) — Additional Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-025.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-025 ' + "Gemini vectorization `colorCount` is `16` for `flat`, `32` for `detailed`, and `16` again for `schematic` or `photorealistic`");
    }


    // This test validates: Gemini vectorization `colorCount` is `16` for `flat`, `32` for `detailed`, and `16` again for `schematic` or `photorealistic`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-026: Editor store uses subscribeWithSelector middleware', async ({ page }) => {
    // Checkpoint 26: Editor store uses `subscribeWithSelector` middleware
    // Section: Quick Test Workflows > Editor Store Details (`editorStore.ts`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Editor store uses `subscribeWithSelector` middleware",
      section: "Quick Test Workflows",
      subsection: "Editor Store Details (`editorStore.ts`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-026.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-026 ' + "Editor store uses `subscribeWithSelector` middleware");
    }


    // This test validates: Editor store uses `subscribeWithSelector` middleware
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-027: Editor store devtools name is finnish-editor-store', async ({ page }) => {
    // Checkpoint 27: Editor store devtools name is `finnish-editor-store`
    // Section: Quick Test Workflows > Editor Store Details (`editorStore.ts`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Editor store devtools name is `finnish-editor-store`",
      section: "Quick Test Workflows",
      subsection: "Editor Store Details (`editorStore.ts`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-027.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-027 ' + "Editor store devtools name is `finnish-editor-store`");
    }


    // This test validates: Editor store devtools name is `finnish-editor-store`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-028: selectObjectsobjectIds action replaces entire selection array', async ({ page }) => {
    // Checkpoint 28: `selectObjects(objectIds)` action replaces entire selection array
    // Section: Quick Test Workflows > Editor Store Details (`editorStore.ts`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`selectObjects(objectIds)` action replaces entire selection array",
      section: "Quick Test Workflows",
      subsection: "Editor Store Details (`editorStore.ts`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-028.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-028 ' + "`selectObjects(objectIds)` action replaces entire selection array");
    }


    // This test validates: `selectObjects(objectIds)` action replaces entire selection array
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-029: addToSelectionobjectId action appends to selection array', async ({ page }) => {
    // Checkpoint 29: `addToSelection(objectId)` action appends to selection array
    // Section: Quick Test Workflows > Editor Store Details (`editorStore.ts`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`addToSelection(objectId)` action appends to selection array",
      section: "Quick Test Workflows",
      subsection: "Editor Store Details (`editorStore.ts`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-029.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-029 ' + "`addToSelection(objectId)` action appends to selection array");
    }


    // This test validates: `addToSelection(objectId)` action appends to selection array
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-030: removeFromSelectionobjectId action filters from selection array', async ({ page }) => {
    // Checkpoint 30: `removeFromSelection(objectId)` action filters from selection array
    // Section: Quick Test Workflows > Editor Store Details (`editorStore.ts`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "`removeFromSelection(objectId)` action filters from selection array",
      section: "Quick Test Workflows",
      subsection: "Editor Store Details (`editorStore.ts`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-030.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-030 ' + "`removeFromSelection(objectId)` action filters from selection array");
    }


    // This test validates: `removeFromSelection(objectId)` action filters from selection array
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-031: Panel title text is AI Image Generation preceded by a SparklesIcon SVG', async ({ page }) => {
    // Checkpoint 31: Panel title text is `AI Image Generation` preceded by a SparklesIcon SVG
    // Section: Quick Test Workflows > AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Panel title text is `AI Image Generation` preceded by a SparklesIcon SVG",
      section: "Quick Test Workflows",
      subsection: "AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-031.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-031 ' + "Panel title text is `AI Image Generation` preceded by a SparklesIcon SVG");
    }


    // This test validates: Panel title text is `AI Image Generation` preceded by a SparklesIcon SVG
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-032: Close button has aria-labelClose', async ({ page }) => {
    // Checkpoint 32: Close button has `aria-label="Close"`
    // Section: Quick Test Workflows > AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "Close button has `aria-label=\"Close\"`",
      section: "Quick Test Workflows",
      subsection: "AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-032.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-032 ' + "Close button has `aria-label=\"Close\"`");
    }


    // This test validates: Close button has `aria-label="Close"`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-033: API key is persisted to localStoragefinnish_fal_api_key', async ({ page }) => {
    // Checkpoint 33: API key is persisted to `localStorage['finnish_fal_api_key']`
    // Section: Quick Test Workflows > AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "API key is persisted to `localStorage['finnish_fal_api_key']`",
      section: "Quick Test Workflows",
      subsection: "AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-033.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-033 ' + "API key is persisted to `localStorage['finnish_fal_api_key']`");
    }


    // This test validates: API key is persisted to `localStorage['finnish_fal_api_key']`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-034: API key input is typepassword with placeholder Enter your falai API key', async ({ page }) => {
    // Checkpoint 34: API key input is `type="password"` with placeholder `Enter your fal.ai API key`
    // Section: Quick Test Workflows > AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)

    // Navigate to the page
    await page.goto('/illustrate', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/illustrate/spec-033');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertIllustrateCheckpoint({
      page,
      description: "API key input is `type=\"password\"` with placeholder `Enter your fal.ai API key`",
      section: "Quick Test Workflows",
      subsection: "AI Image Generation Tool — Full UI Details (`AIGenerationTool.tsx`)",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-034.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled illustrate checkpoint: cp-034 ' + "API key input is `type=\"password\"` with placeholder `Enter your fal.ai API key`");
    }


    // This test validates: API key input is `type="password"` with placeholder `Enter your fal.ai API key`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });
});
