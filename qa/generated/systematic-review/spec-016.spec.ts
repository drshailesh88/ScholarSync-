/**
 * Auto-generated Playwright test for systematic-review/spec-016
 * Source: e2e/specs/systematic-review/spec-016.md
 * Generated: 2026-03-15T04:37:09.733Z
 *
 * Each test case corresponds to one checkbox in the spec file.
 * The controller (qa/controller.ts) uses Playwright JSON output
 * and artifact existence to determine verdicts.
 *
 * DO NOT EDIT — regenerate with: npx tsx qa/spec-to-playwright.ts systematic-review spec-016
 */

import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';

















import { assertSystematicReviewCheckpoint } from '../../module-assertions/systematic-review';


test.describe('systematic-review / spec-016', () => {
  test.beforeEach(async ({ page }) => {
    // Dev mode auth bypass — no Clerk needed
    const baseUrl = process.env.BASE_URL ?? 'http://127.0.0.1:3001';
    const url = new URL(baseUrl);
    await page.context().addCookies([{
      name: '__playwright',
      value: 'true',
      domain: url.hostname,
      path: '/',
    }]);
  });

  test('cp-000: PRISMA Flow error text is Failed to generate PRISMA flow diagram Please try agai', async ({ page }) => {
    // Checkpoint 0: PRISMA Flow error text is `Failed to generate PRISMA flow diagram. Please try again.`
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "PRISMA Flow error text is `Failed to generate PRISMA flow diagram. Please try again.`",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-000.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-000 ' + "PRISMA Flow error text is `Failed to generate PRISMA flow diagram. Please try again.`");
    }


    // This test validates: PRISMA Flow error text is `Failed to generate PRISMA flow diagram. Please try again.`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-001: PRISMA checklist default variant is PRISMA 2020', async ({ page }) => {
    // Checkpoint 1: PRISMA checklist default variant is `PRISMA 2020`
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "PRISMA checklist default variant is `PRISMA 2020`",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-001.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-001 ' + "PRISMA checklist default variant is `PRISMA 2020`");
    }


    // This test validates: PRISMA checklist default variant is `PRISMA 2020`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-002: PRISMA checklist variant buttons are PRISMA 2020 PRISMA-S Search and PRISMA-NMA ', async ({ page }) => {
    // Checkpoint 2: PRISMA checklist variant buttons are `PRISMA 2020`, `PRISMA-S (Search)`, and `PRISMA-NMA (Network MA)`
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "PRISMA checklist variant buttons are `PRISMA 2020`, `PRISMA-S (Search)`, and `PRISMA-NMA (Network MA)`",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-002.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-002 ' + "PRISMA checklist variant buttons are `PRISMA 2020`, `PRISMA-S (Search)`, and `PRISMA-NMA (Network MA)`");
    }


    // This test validates: PRISMA checklist variant buttons are `PRISMA 2020`, `PRISMA-S (Search)`, and `PRISMA-NMA (Network MA)`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-003: Switching checklist variant resets the status filter to all', async ({ page }) => {
    // Checkpoint 3: Switching checklist variant resets the status filter to `all`
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Switching checklist variant resets the status filter to `all`",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-003.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-003 ' + "Switching checklist variant resets the status filter to `all`");
    }


    // This test validates: Switching checklist variant resets the status filter to `all`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-004: Switching checklist variant collapses all expanded items', async ({ page }) => {
    // Checkpoint 4: Switching checklist variant collapses all expanded items
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Switching checklist variant collapses all expanded items",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-004.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-004 ' + "Switching checklist variant collapses all expanded items");
    }


    // This test validates: Switching checklist variant collapses all expanded items
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-005: Manuscript textarea placeholder is Paste your manuscript text here minimum 100 c', async ({ page }) => {
    // Checkpoint 5: Manuscript textarea placeholder is `Paste your manuscript text here (minimum 100 characters)...`
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Manuscript textarea placeholder is `Paste your manuscript text here (minimum 100 characters)...`",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-005.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-005 ' + "Manuscript textarea placeholder is `Paste your manuscript text here (minimum 100 characters)...`");
    }


    // This test validates: Manuscript textarea placeholder is `Paste your manuscript text here (minimum 100 characters)...`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-006: Character counter uses localized formatting and remains visible before verificat', async ({ page }) => {
    // Checkpoint 6: Character counter uses localized formatting and remains visible before verification
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Character counter uses localized formatting and remains visible before verification",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-006.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-006 ' + "Character counter uses localized formatting and remains visible before verification");
    }


    // This test validates: Character counter uses localized formatting and remains visible before verification
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-007: Verify button is disabled until manuscript text reaches 100 characters', async ({ page }) => {
    // Checkpoint 7: Verify button is disabled until manuscript text reaches 100 characters
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Verify button is disabled until manuscript text reaches 100 characters",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-007.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-007 ' + "Verify button is disabled until manuscript text reaches 100 characters");
    }


    // This test validates: Verify button is disabled until manuscript text reaches 100 characters
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-008: Verify button label includes the active variant short label eg Verify PRISMA 202', async ({ page }) => {
    // Checkpoint 8: Verify button label includes the active variant short label, e.g. `Verify PRISMA 2020 Compliance`
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Verify button label includes the active variant short label, e.g. `Verify PRISMA 2020 Compliance`",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-008.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-008 ' + "Verify button label includes the active variant short label, e.g. `Verify PRISMA 2020 Compliance`");
    }


    // This test validates: Verify button label includes the active variant short label, e.g. `Verify PRISMA 2020 Compliance`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-009: In-flight verify label uses variant-specific copy such as Verifying 27 items', async ({ page }) => {
    // Checkpoint 9: In-flight verify label uses variant-specific copy such as `Verifying 27 items...`
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "In-flight verify label uses variant-specific copy such as `Verifying 27 items...`",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-009.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-009 ' + "In-flight verify label uses variant-specific copy such as `Verifying 27 items...`");
    }


    // This test validates: In-flight verify label uses variant-specific copy such as `Verifying 27 items...`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-010: Summary stat cards are clickable filters that toggle each status onoff', async ({ page }) => {
    // Checkpoint 10: Summary stat cards are clickable filters that toggle each status on/off
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Summary stat cards are clickable filters that toggle each status on/off",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-010.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-010 ' + "Summary stat cards are clickable filters that toggle each status on/off");
    }


    // This test validates: Summary stat cards are clickable filters that toggle each status on/off
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-011: Expanding all checklist items uses the item numbers from the current filtered re', async ({ page }) => {
    // Checkpoint 11: Expanding all checklist items uses the item numbers from the current filtered result set
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Expanding all checklist items uses the item numbers from the current filtered result set",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-011.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-011 ' + "Expanding all checklist items uses the item numbers from the current filtered result set");
    }


    // This test validates: Expanding all checklist items uses the item numbers from the current filtered result set
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-012: Checklist export filename changes by variant prisma-2020-checklistcsv prisma-s-c', async ({ page }) => {
    // Checkpoint 12: Checklist export filename changes by variant (`prisma-2020-checklist.csv`, `prisma-s-checklist.csv`, `prisma-nma-checklist.csv`)
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Checklist export filename changes by variant (`prisma-2020-checklist.csv`, `prisma-s-checklist.csv`, `prisma-nma-checklist.csv`)",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-012.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-012 ' + "Checklist export filename changes by variant (`prisma-2020-checklist.csv`, `prisma-s-checklist.csv`, `prisma-nma-checklist.csv`)");
    }


    // This test validates: Checklist export filename changes by variant (`prisma-2020-checklist.csv`, `prisma-s-checklist.csv`, `prisma-nma-checklist.csv`)
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-013: Found and Suggestion rows render only when those values are non-empty', async ({ page }) => {
    // Checkpoint 13: `Found:` and `Suggestion:` rows render only when those values are non-empty
    // Section: Quick Test Workflows > PRISMA Flow and PRISMA Checklist — Combined Tab Details

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "`Found:` and `Suggestion:` rows render only when those values are non-empty",
      section: "Quick Test Workflows",
      subsection: "PRISMA Flow and PRISMA Checklist — Combined Tab Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-013.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-013 ' + "`Found:` and `Suggestion:` rows render only when those values are non-empty");
    }


    // This test validates: `Found:` and `Suggestion:` rows render only when those values are non-empty
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-014: Protocol panel preloads the saved PROSPERO registration id from reviewConfigprot', async ({ page }) => {
    // Checkpoint 14: Protocol panel preloads the saved PROSPERO registration id from `reviewConfig.protocolRegistration`
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Protocol panel preloads the saved PROSPERO registration id from `reviewConfig.protocolRegistration`",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-014.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-014 ' + "Protocol panel preloads the saved PROSPERO registration id from `reviewConfig.protocolRegistration`");
    }


    // This test validates: Protocol panel preloads the saved PROSPERO registration id from `reviewConfig.protocolRegistration`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-015: PROSPERO ID placeholder is eg CRD42024XXXXXX', async ({ page }) => {
    // Checkpoint 15: PROSPERO ID placeholder is `e.g. CRD42024XXXXXX`
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "PROSPERO ID placeholder is `e.g. CRD42024XXXXXX`",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-015.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-015 ' + "PROSPERO ID placeholder is `e.g. CRD42024XXXXXX`");
    }


    // This test validates: PROSPERO ID placeholder is `e.g. CRD42024XXXXXX`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-016: Save PROSPERO ID button is disabled when the trimmed field is empty', async ({ page }) => {
    // Checkpoint 16: Save PROSPERO ID button is disabled when the trimmed field is empty
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Save PROSPERO ID button is disabled when the trimmed field is empty",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-016.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-016 ' + "Save PROSPERO ID button is disabled when the trimmed field is empty");
    }


    // This test validates: Save PROSPERO ID button is disabled when the trimmed field is empty
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-017: Save PROSPERO ID success changes the button label to Saved temporarily', async ({ page }) => {
    // Checkpoint 17: Save PROSPERO ID success changes the button label to `Saved` temporarily
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Save PROSPERO ID success changes the button label to `Saved` temporarily",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-017.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-017 ' + "Save PROSPERO ID success changes the button label to `Saved` temporarily");
    }


    // This test validates: Save PROSPERO ID success changes the button label to `Saved` temporarily
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-018: Generate Protocol button label changes to Generating protocol 16 sections while ', async ({ page }) => {
    // Checkpoint 18: Generate Protocol button label changes to `Generating protocol (16 sections)...` while running
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Generate Protocol button label changes to `Generating protocol (16 sections)...` while running",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-018.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-018 ' + "Generate Protocol button label changes to `Generating protocol (16 sections)...` while running");
    }


    // This test validates: Generate Protocol button label changes to `Generating protocol (16 sections)...` while running
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-019: Successful protocol generation expands all returned sections by default', async ({ page }) => {
    // Checkpoint 19: Successful protocol generation expands all returned sections by default
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Successful protocol generation expands all returned sections by default",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-019.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-019 ' + "Successful protocol generation expands all returned sections by default");
    }


    // This test validates: Successful protocol generation expands all returned sections by default
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-020: Regenerate in the controls bar clears the generated protocol from view instead o', async ({ page }) => {
    // Checkpoint 20: `Regenerate` in the controls bar clears the generated protocol from view instead of calling the API immediately
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "`Regenerate` in the controls bar clears the generated protocol from view instead of calling the API immediately",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-020.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-020 ' + "`Regenerate` in the controls bar clears the generated protocol from view instead of calling the API immediately");
    }


    // This test validates: `Regenerate` in the controls bar clears the generated protocol from view instead of calling the API immediately
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-021: Protocol copy action concatenates every section title and content into one clipb', async ({ page }) => {
    // Checkpoint 21: Protocol copy action concatenates every section title and content into one clipboard payload
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Protocol copy action concatenates every section title and content into one clipboard payload",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-021.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-021 ' + "Protocol copy action concatenates every section title and content into one clipboard payload");
    }


    // This test validates: Protocol copy action concatenates every section title and content into one clipboard payload
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-022: Protocol TXT export filename is protocoltxt', async ({ page }) => {
    // Checkpoint 22: Protocol TXT export filename is `protocol.txt`
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Protocol TXT export filename is `protocol.txt`",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-022.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-022 ' + "Protocol TXT export filename is `protocol.txt`");
    }


    // This test validates: Protocol TXT export filename is `protocol.txt`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-023: Protocol HTML export filename is protocolhtml', async ({ page }) => {
    // Checkpoint 23: Protocol HTML export filename is `protocol.html`
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Protocol HTML export filename is `protocol.html`",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-023.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-023 ' + "Protocol HTML export filename is `protocol.html`");
    }


    // This test validates: Protocol HTML export filename is `protocol.html`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-024: Protocol HTML export button title mentions printing to PDF via the browser', async ({ page }) => {
    // Checkpoint 24: Protocol HTML export button title mentions printing to PDF via the browser
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Protocol HTML export button title mentions printing to PDF via the browser",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-024.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-024 ' + "Protocol HTML export button title mentions printing to PDF via the browser");
    }


    // This test validates: Protocol HTML export button title mentions printing to PDF via the browser
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-025: Section edit mode is per-section and stores edits only in local component state ', async ({ page }) => {
    // Checkpoint 25: Section edit mode is per-section and stores edits only in local component state until export/copy
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Section edit mode is per-section and stores edits only in local component state until export/copy",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-025.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-025 ' + "Section edit mode is per-section and stores edits only in local component state until export/copy");
    }


    // This test validates: Section edit mode is per-section and stores edits only in local component state until export/copy
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-026: PROSPERO helper loads fields on mount from apisystematic-reviewprosperoprojectId', async ({ page }) => {
    // Checkpoint 26: PROSPERO helper loads fields on mount from `/api/systematic-review/prospero?projectId={projectId}`
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "PROSPERO helper loads fields on mount from `/api/systematic-review/prospero?projectId={projectId}`",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-026.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-026 ' + "PROSPERO helper loads fields on mount from `/api/systematic-review/prospero?projectId={projectId}`");
    }


    // This test validates: PROSPERO helper loads fields on mount from `/api/systematic-review/prospero?projectId={projectId}`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-027: PROSPERO progress text shows filledCounttotalCount fields completed', async ({ page }) => {
    // Checkpoint 27: PROSPERO progress text shows `{filledCount}/{totalCount} fields completed`
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "PROSPERO progress text shows `{filledCount}/{totalCount} fields completed`",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-027.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-027 ' + "PROSPERO progress text shows `{filledCount}/{totalCount} fields completed`");
    }


    // This test validates: PROSPERO progress text shows `{filledCount}/{totalCount} fields completed`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-028: PROSPERO progress bar color changes by completion percentage threshold', async ({ page }) => {
    // Checkpoint 28: PROSPERO progress bar color changes by completion percentage threshold
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "PROSPERO progress bar color changes by completion percentage threshold",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-028.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-028 ' + "PROSPERO progress bar color changes by completion percentage threshold");
    }


    // This test validates: PROSPERO progress bar color changes by completion percentage threshold
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-029: Copy All button text changes to Copied temporarily after successful copy', async ({ page }) => {
    // Checkpoint 29: `Copy All` button text changes to `Copied!` temporarily after successful copy
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "`Copy All` button text changes to `Copied!` temporarily after successful copy",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-029.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-029 ' + "`Copy All` button text changes to `Copied!` temporarily after successful copy");
    }


    // This test validates: `Copy All` button text changes to `Copied!` temporarily after successful copy
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-030: Download as TXT button text changes to Downloading while POST export is running', async ({ page }) => {
    // Checkpoint 30: `Download as TXT` button text changes to `Downloading…` while POST export is running
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "`Download as TXT` button text changes to `Downloading…` while POST export is running",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-030.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-030 ' + "`Download as TXT` button text changes to `Downloading…` while POST export is running");
    }


    // This test validates: `Download as TXT` button text changes to `Downloading…` while POST export is running
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-031: Refresh from project reloads the 22 PROSPERO fields from the server', async ({ page }) => {
    // Checkpoint 31: `Refresh from project` reloads the 22 PROSPERO fields from the server
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "`Refresh from project` reloads the 22 PROSPERO fields from the server",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-031.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-031 ' + "`Refresh from project` reloads the 22 PROSPERO fields from the server");
    }


    // This test validates: `Refresh from project` reloads the 22 PROSPERO fields from the server
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-032: Each PROSPERO field header shows a green filled indicator or red required-empty ', async ({ page }) => {
    // Checkpoint 32: Each PROSPERO field header shows a green filled indicator or red required-empty indicator
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Each PROSPERO field header shows a green filled indicator or red required-empty indicator",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-032.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-032 ' + "Each PROSPERO field header shows a green filled indicator or red required-empty indicator");
    }


    // This test validates: Each PROSPERO field header shows a green filled indicator or red required-empty indicator
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-033: Each PROSPERO field shows an Auto or Manual source badge', async ({ page }) => {
    // Checkpoint 33: Each PROSPERO field shows an `Auto` or `Manual` source badge
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Each PROSPERO field shows an `Auto` or `Manual` source badge",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-033.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-033 ' + "Each PROSPERO field shows an `Auto` or `Manual` source badge");
    }


    // This test validates: Each PROSPERO field shows an `Auto` or `Manual` source badge
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-034: Empty manual field placeholder is Enter field name', async ({ page }) => {
    // Checkpoint 34: Empty manual field placeholder is `Enter {field name}…`
    // Section: Quick Test Workflows > Protocol and PROSPERO Panels — Detailed Behavior

    // Navigate to the page
    await page.goto('/systematic-review', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/systematic-review/spec-016');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertSystematicReviewCheckpoint({
      page,
      description: "Empty manual field placeholder is `Enter {field name}…`",
      section: "Quick Test Workflows",
      subsection: "Protocol and PROSPERO Panels — Detailed Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-034.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled systematic-review checkpoint: cp-034 ' + "Empty manual field placeholder is `Enter {field name}…`");
    }


    // This test validates: Empty manual field placeholder is `Enter {field name}…`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });
});
