/**
 * Auto-generated Playwright test for notebook/spec-015
 * Source: e2e/specs/notebook/spec-015.md
 * Generated: 2026-03-14T10:50:54.010Z
 *
 * Each test case corresponds to one checkbox in the spec file.
 * The controller (qa/controller.ts) uses Playwright JSON output
 * and artifact existence to determine verdicts.
 *
 * DO NOT EDIT — regenerate with: npx tsx qa/spec-to-playwright.ts notebook spec-015
 */

import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';








import { assertNotebookCheckpoint } from '../../module-assertions/notebook';











test.describe('notebook / spec-015', () => {
  test.beforeEach(async ({ page }) => {
    // Dev mode auth bypass — no Clerk needed
    const baseUrl = process.env.BASE_URL ?? 'http://127.0.0.1:3001';
    const url = new URL(baseUrl);
    await page.context().addCookies([{
      name: '__playwright',
      value: 'true',
      domain: url.hostname,
      path: '/',
    }]);
  });

  test('cp-000: Stream read timeouts are handled separately from initial request failures using ', async ({ page }) => {
    // Checkpoint 0: Stream read timeouts are handled separately from initial request failures using a per-read 30 second timeout race
    // Section: Quick Test Workflows > Chat Send, Streaming, and Error Handling Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Stream read timeouts are handled separately from initial request failures using a per-read 30 second timeout race",
      section: "Quick Test Workflows",
      subsection: "Chat Send, Streaming, and Error Handling Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-000.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-000 ' + "Stream read timeouts are handled separately from initial request failures using a per-read 30 second timeout race");
    }


    // This test validates: Stream read timeouts are handled separately from initial request failures using a per-read 30 second timeout race
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-001: Stream timeouts append the assistant message The response timed out Please try a', async ({ page }) => {
    // Checkpoint 1: Stream timeouts append the assistant message `The response timed out. Please try again or ask a simpler question.`
    // Section: Quick Test Workflows > Chat Send, Streaming, and Error Handling Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Stream timeouts append the assistant message `The response timed out. Please try again or ask a simpler question.`",
      section: "Quick Test Workflows",
      subsection: "Chat Send, Streaming, and Error Handling Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-001.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-001 ' + "Stream timeouts append the assistant message `The response timed out. Please try again or ask a simpler question.`");
    }


    // This test validates: Stream timeouts append the assistant message `The response timed out. Please try again or ask a simpler question.`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-002: Non-timeout runtime failures append the assistant message Something went wrong P', async ({ page }) => {
    // Checkpoint 2: Non-timeout runtime failures append the assistant message `Something went wrong. Please try again.`
    // Section: Quick Test Workflows > Chat Send, Streaming, and Error Handling Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Non-timeout runtime failures append the assistant message `Something went wrong. Please try again.`",
      section: "Quick Test Workflows",
      subsection: "Chat Send, Streaming, and Error Handling Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-002.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-002 ' + "Non-timeout runtime failures append the assistant message `Something went wrong. Please try again.`");
    }


    // This test validates: Non-timeout runtime failures append the assistant message `Something went wrong. Please try again.`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-003: Request cleanup always clears the outer timeout and flips isLoading false in fin', async ({ page }) => {
    // Checkpoint 3: Request cleanup always clears the outer timeout and flips `isLoading` false in `finally`
    // Section: Quick Test Workflows > Chat Send, Streaming, and Error Handling Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Request cleanup always clears the outer timeout and flips `isLoading` false in `finally`",
      section: "Quick Test Workflows",
      subsection: "Chat Send, Streaming, and Error Handling Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-003.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-003 ' + "Request cleanup always clears the outer timeout and flips `isLoading` false in `finally`");
    }


    // This test validates: Request cleanup always clears the outer timeout and flips `isLoading` false in `finally`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-004: Notebook reads cited source metadata from the X-RAG-Sources response header', async ({ page }) => {
    // Checkpoint 4: Notebook reads cited source metadata from the `X-RAG-Sources` response header
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Notebook reads cited source metadata from the `X-RAG-Sources` response header",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-004.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-004 ' + "Notebook reads cited source metadata from the `X-RAG-Sources` response header");
    }


    // This test validates: Notebook reads cited source metadata from the `X-RAG-Sources` response header
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-005: Parsing valid X-RAG-Sources sets both currentSources and showSourcesPanel', async ({ page }) => {
    // Checkpoint 5: Parsing valid `X-RAG-Sources` sets both `currentSources` and `showSourcesPanel`
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Parsing valid `X-RAG-Sources` sets both `currentSources` and `showSourcesPanel`",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-005.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-005 ' + "Parsing valid `X-RAG-Sources` sets both `currentSources` and `showSourcesPanel`");
    }


    // This test validates: Parsing valid `X-RAG-Sources` sets both `currentSources` and `showSourcesPanel`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-006: Invalid X-RAG-Sources JSON is ignored without UI error', async ({ page }) => {
    // Checkpoint 6: Invalid `X-RAG-Sources` JSON is ignored without UI error
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Invalid `X-RAG-Sources` JSON is ignored without UI error",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-006.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-006 ' + "Invalid `X-RAG-Sources` JSON is ignored without UI error");
    }


    // This test validates: Invalid `X-RAG-Sources` JSON is ignored without UI error
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-007: Notebook reads coverage data from the X-RAG-Coverage response header', async ({ page }) => {
    // Checkpoint 7: Notebook reads coverage data from the `X-RAG-Coverage` response header
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Notebook reads coverage data from the `X-RAG-Coverage` response header",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-007.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-007 ' + "Notebook reads coverage data from the `X-RAG-Coverage` response header");
    }


    // This test validates: Notebook reads coverage data from the `X-RAG-Coverage` response header
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-008: Missing coverage headers explicitly clear any prior coverage report', async ({ page }) => {
    // Checkpoint 8: Missing coverage headers explicitly clear any prior coverage report
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Missing coverage headers explicitly clear any prior coverage report",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-008.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-008 ' + "Missing coverage headers explicitly clear any prior coverage report");
    }


    // This test validates: Missing coverage headers explicitly clear any prior coverage report
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-009: Coverage badge renders only when coverageReporttotalPapers 1', async ({ page }) => {
    // Checkpoint 9: Coverage badge renders only when `coverageReport.totalPapers > 1`
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Coverage badge renders only when `coverageReport.totalPapers > 1`",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-009.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-009 ' + "Coverage badge renders only when `coverageReport.totalPapers > 1`");
    }


    // This test validates: Coverage badge renders only when `coverageReport.totalPapers > 1`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-010: Coverage badge uses green styling only for coverageRatio 1', async ({ page }) => {
    // Checkpoint 10: Coverage badge uses green styling only for `coverageRatio === 1`
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Coverage badge uses green styling only for `coverageRatio === 1`",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-010.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-010 ' + "Coverage badge uses green styling only for `coverageRatio === 1`");
    }


    // This test validates: Coverage badge uses green styling only for `coverageRatio === 1`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-011: Coverage badge uses amber styling for ratios 05 and red styling below that thres', async ({ page }) => {
    // Checkpoint 11: Coverage badge uses amber styling for ratios `>= 0.5` and red styling below that threshold
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Coverage badge uses amber styling for ratios `>= 0.5` and red styling below that threshold",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-011.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-011 ' + "Coverage badge uses amber styling for ratios `>= 0.5` and red styling below that threshold");
    }


    // This test validates: Coverage badge uses amber styling for ratios `>= 0.5` and red styling below that threshold
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-012: Coverage badge truncates unused-paper titles at the first colon when present oth', async ({ page }) => {
    // Checkpoint 12: Coverage badge truncates unused-paper titles at the first colon when present, otherwise at 30 characters
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Coverage badge truncates unused-paper titles at the first colon when present, otherwise at 30 characters",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-012.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-012 ' + "Coverage badge truncates unused-paper titles at the first colon when present, otherwise at 30 characters");
    }


    // This test validates: Coverage badge truncates unused-paper titles at the first colon when present, otherwise at 30 characters
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-013: Follow-up suggestions are requested only when the assistant response has at leas', async ({ page }) => {
    // Checkpoint 13: Follow-up suggestions are requested only when the assistant response has at least 100 trimmed characters
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Follow-up suggestions are requested only when the assistant response has at least 100 trimmed characters",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-013.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-013 ' + "Follow-up suggestions are requested only when the assistant response has at least 100 trimmed characters");
    }


    // This test validates: Follow-up suggestions are requested only when the assistant response has at least 100 trimmed characters
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-014: Follow-up suggestion requests include responseText sourceTitles userQuery and cu', async ({ page }) => {
    // Checkpoint 14: Follow-up suggestion requests include `responseText`, `sourceTitles`, `userQuery`, and current notebook `mode`
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Follow-up suggestion requests include `responseText`, `sourceTitles`, `userQuery`, and current notebook `mode`",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-014.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-014 ' + "Follow-up suggestion requests include `responseText`, `sourceTitles`, `userQuery`, and current notebook `mode`");
    }


    // This test validates: Follow-up suggestion requests include `responseText`, `sourceTitles`, `userQuery`, and current notebook `mode`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-015: Stale follow-up suggestion responses are dropped by comparing suggestionRequestI', async ({ page }) => {
    // Checkpoint 15: Stale follow-up suggestion responses are dropped by comparing `suggestionRequestIdRef`
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Stale follow-up suggestion responses are dropped by comparing `suggestionRequestIdRef`",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-015.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-015 ' + "Stale follow-up suggestion responses are dropped by comparing `suggestionRequestIdRef`");
    }


    // This test validates: Stale follow-up suggestion responses are dropped by comparing `suggestionRequestIdRef`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-016: Follow-up suggestion chips render only for the latest assistant message whose id', async ({ page }) => {
    // Checkpoint 16: Follow-up suggestion chips render only for the latest assistant message whose id matches `suggestionsForMessageId`
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Follow-up suggestion chips render only for the latest assistant message whose id matches `suggestionsForMessageId`",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-016.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-016 ' + "Follow-up suggestion chips render only for the latest assistant message whose id matches `suggestionsForMessageId`");
    }


    // This test validates: Follow-up suggestion chips render only for the latest assistant message whose id matches `suggestionsForMessageId`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-017: Learn-mode suggestion chips switch to amber-tinted styling while research-mode c', async ({ page }) => {
    // Checkpoint 17: Learn-mode suggestion chips switch to amber-tinted styling while research-mode chips use neutral surface styling
    // Section: Quick Test Workflows > RAG Headers, Coverage, and Follow-Up Suggestion Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Learn-mode suggestion chips switch to amber-tinted styling while research-mode chips use neutral surface styling",
      section: "Quick Test Workflows",
      subsection: "RAG Headers, Coverage, and Follow-Up Suggestion Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-017.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-017 ' + "Learn-mode suggestion chips switch to amber-tinted styling while research-mode chips use neutral surface styling");
    }


    // This test validates: Learn-mode suggestion chips switch to amber-tinted styling while research-mode chips use neutral surface styling
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-018: Chat log container uses both rolelog and aria-livepolite', async ({ page }) => {
    // Checkpoint 18: Chat log container uses both `role="log"` and `aria-live="polite"`
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Chat log container uses both `role=\"log\"` and `aria-live=\"polite\"`",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-018.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-018 ' + "Chat log container uses both `role=\"log\"` and `aria-live=\"polite\"`");
    }


    // This test validates: Chat log container uses both `role="log"` and `aria-live="polite"`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-019: Assistant citations render only when the message role is assistant and the messa', async ({ page }) => {
    // Checkpoint 19: Assistant citations render only when the message role is `assistant` and the message has a non-empty `sources` array
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Assistant citations render only when the message role is `assistant` and the message has a non-empty `sources` array",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-019.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-019 ' + "Assistant citations render only when the message role is `assistant` and the message has a non-empty `sources` array");
    }


    // This test validates: Assistant citations render only when the message role is `assistant` and the message has a non-empty `sources` array
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-020: Citation chips show a FilePdf icon plus a shortened paper title and optional pag', async ({ page }) => {
    // Checkpoint 20: Citation chips show a `FilePdf` icon plus a shortened paper title and optional page label
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Citation chips show a `FilePdf` icon plus a shortened paper title and optional page label",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-020.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-020 ' + "Citation chips show a `FilePdf` icon plus a shortened paper title and optional page label");
    }


    // This test validates: Citation chips show a `FilePdf` icon plus a shortened paper title and optional page label
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-021: Clicking a citation chip stores its source index in highlightedSource', async ({ page }) => {
    // Checkpoint 21: Clicking a citation chip stores its source index in `highlightedSource`
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Clicking a citation chip stores its source index in `highlightedSource`",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-021.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-021 ' + "Clicking a citation chip stores its source index in `highlightedSource`");
    }


    // This test validates: Clicking a citation chip stores its source index in `highlightedSource`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-022: Citation-chip tooltip includes page number and section type when present', async ({ page }) => {
    // Checkpoint 22: Citation-chip tooltip includes page number and section type when present
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Citation-chip tooltip includes page number and section type when present",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-022.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-022 ' + "Citation-chip tooltip includes page number and section type when present");
    }


    // This test validates: Citation-chip tooltip includes page number and section type when present
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-023: Copy action strips bracketed citation markers like 1 before writing to the clipb', async ({ page }) => {
    // Checkpoint 23: Copy action strips bracketed citation markers like `[1]` before writing to the clipboard
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Copy action strips bracketed citation markers like `[1]` before writing to the clipboard",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-023.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-023 ' + "Copy action strips bracketed citation markers like `[1]` before writing to the clipboard");
    }


    // This test validates: Copy action strips bracketed citation markers like `[1]` before writing to the clipboard
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-024: Copy action also collapses repeated spaces before copying', async ({ page }) => {
    // Checkpoint 24: Copy action also collapses repeated spaces before copying
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Copy action also collapses repeated spaces before copying",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-024.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-024 ' + "Copy action also collapses repeated spaces before copying");
    }


    // This test validates: Copy action also collapses repeated spaces before copying
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-025: Copy success shows a green check icon for 2 seconds only on the copied message r', async ({ page }) => {
    // Checkpoint 25: Copy success shows a green check icon for 2 seconds only on the copied message row
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Copy success shows a green check icon for 2 seconds only on the copied message row",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-025.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-025 ' + "Copy success shows a green check icon for 2 seconds only on the copied message row");
    }


    // This test validates: Copy success shows a green check icon for 2 seconds only on the copied message row
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-026: Feedback actions allow toggle-off behavior by clicking the already-selected rati', async ({ page }) => {
    // Checkpoint 26: Feedback actions allow toggle-off behavior by clicking the already-selected rating again
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Feedback actions allow toggle-off behavior by clicking the already-selected rating again",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-026.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-026 ' + "Feedback actions allow toggle-off behavior by clicking the already-selected rating again");
    }


    // This test validates: Feedback actions allow toggle-off behavior by clicking the already-selected rating again
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-027: Helpful state uses green tint with ThumbsUp weightfill', async ({ page }) => {
    // Checkpoint 27: Helpful state uses green tint with `ThumbsUp` `weight="fill"`
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Helpful state uses green tint with `ThumbsUp` `weight=\"fill\"`",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-027.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-027 ' + "Helpful state uses green tint with `ThumbsUp` `weight=\"fill\"`");
    }


    // This test validates: Helpful state uses green tint with `ThumbsUp` `weight="fill"`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-028: Unhelpful state uses red tint with ThumbsDown weightfill', async ({ page }) => {
    // Checkpoint 28: Unhelpful state uses red tint with `ThumbsDown` `weight="fill"`
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Unhelpful state uses red tint with `ThumbsDown` `weight=\"fill\"`",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-028.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-028 ' + "Unhelpful state uses red tint with `ThumbsDown` `weight=\"fill\"`");
    }


    // This test validates: Unhelpful state uses red tint with `ThumbsDown` `weight="fill"`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-029: Feedback submission is attempted only when the message id can be parsed into a p', async ({ page }) => {
    // Checkpoint 29: Feedback submission is attempted only when the message id can be parsed into a positive numeric database id
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Feedback submission is attempted only when the message id can be parsed into a positive numeric database id",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-029.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-029 ' + "Feedback submission is attempted only when the message id can be parsed into a positive numeric database id");
    }


    // This test validates: Feedback submission is attempted only when the message id can be parsed into a positive numeric database id
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-030: Failed feedback submissions do not roll back local UI state they only log to the', async ({ page }) => {
    // Checkpoint 30: Failed feedback submissions do not roll back local UI state; they only log to the console
    // Section: Quick Test Workflows > Message Rendering, Copy, and Feedback Details

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Failed feedback submissions do not roll back local UI state; they only log to the console",
      section: "Quick Test Workflows",
      subsection: "Message Rendering, Copy, and Feedback Details",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-030.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-030 ' + "Failed feedback submissions do not roll back local UI state; they only log to the console");
    }


    // This test validates: Failed feedback submissions do not roll back local UI state; they only log to the console
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-031: Sources-cited toggle label is Sources cited N', async ({ page }) => {
    // Checkpoint 31: Sources-cited toggle label is `Sources cited (N)`
    // Section: Quick Test Workflows > Source Citations Panel and PDF Jump Behavior

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Sources-cited toggle label is `Sources cited (N)`",
      section: "Quick Test Workflows",
      subsection: "Source Citations Panel and PDF Jump Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-031.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-031 ' + "Sources-cited toggle label is `Sources cited (N)`");
    }


    // This test validates: Sources-cited toggle label is `Sources cited (N)`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-032: Sources panel uses currentSourceslength so duplicate chunk references can increa', async ({ page }) => {
    // Checkpoint 32: Sources panel uses `currentSources.length`, so duplicate chunk references can increase the displayed count
    // Section: Quick Test Workflows > Source Citations Panel and PDF Jump Behavior

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Sources panel uses `currentSources.length`, so duplicate chunk references can increase the displayed count",
      section: "Quick Test Workflows",
      subsection: "Source Citations Panel and PDF Jump Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-032.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-032 ' + "Sources panel uses `currentSources.length`, so duplicate chunk references can increase the displayed count");
    }


    // This test validates: Sources panel uses `currentSources.length`, so duplicate chunk references can increase the displayed count
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-033: Highlighted source rows use a brand borderbackground state tied to highlightedSo', async ({ page }) => {
    // Checkpoint 33: Highlighted source rows use a brand border/background state tied to `highlightedSource === src.sourceIndex`
    // Section: Quick Test Workflows > Source Citations Panel and PDF Jump Behavior

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Highlighted source rows use a brand border/background state tied to `highlightedSource === src.sourceIndex`",
      section: "Quick Test Workflows",
      subsection: "Source Citations Panel and PDF Jump Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-033.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-033 ' + "Highlighted source rows use a brand border/background state tied to `highlightedSource === src.sourceIndex`");
    }


    // This test validates: Highlighted source rows use a brand border/background state tied to `highlightedSource === src.sourceIndex`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-034: Clicking a citation to a URL-backed source opens the original URL in a new tab i', async ({ page }) => {
    // Checkpoint 34: Clicking a citation to a URL-backed source opens the original URL in a new tab instead of the PDF viewer
    // Section: Quick Test Workflows > Source Citations Panel and PDF Jump Behavior

    // Navigate to the page
    await page.goto('/notebook', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/notebook/spec-015');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertNotebookCheckpoint({
      page,
      description: "Clicking a citation to a URL-backed source opens the original URL in a new tab instead of the PDF viewer",
      section: "Quick Test Workflows",
      subsection: "Source Citations Panel and PDF Jump Behavior",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-034.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled notebook checkpoint: cp-034 ' + "Clicking a citation to a URL-backed source opens the original URL in a new tab instead of the PDF viewer");
    }


    // This test validates: Clicking a citation to a URL-backed source opens the original URL in a new tab instead of the PDF viewer
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });
});
