/**
 * Auto-generated Playwright test for editor/spec-022
 * Source: e2e/specs/editor/spec-022.md
 * Generated: 2026-03-15T16:51:07.036Z
 *
 * Each test case corresponds to one checkbox in the spec file.
 * The controller (qa/controller.ts) uses Playwright JSON output
 * and artifact existence to determine verdicts.
 *
 * DO NOT EDIT — regenerate with: npx tsx qa/spec-to-playwright.ts editor spec-022
 */

import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';




import { assertEditorCheckpoint } from '../../module-assertions/editor';















test.describe('editor / spec-022', () => {
  test.beforeEach(async ({ page }) => {
    // Dev mode auth bypass — no Clerk needed
    const baseUrl = process.env.BASE_URL ?? 'http://127.0.0.1:3001';
    const url = new URL(baseUrl);
    await page.context().addCookies([{
      name: '__playwright',
      value: 'true',
      domain: url.hostname,
      path: '/',
    }]);
  });

  test('cp-000: Citation nodes also support an overrides attribute that defaults to null', async ({ page }) => {
    // Checkpoint 0: Citation nodes also support an `overrides` attribute that defaults to `null`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation nodes also support an `overrides` attribute that defaults to `null`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-000.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-000 ' + "Citation nodes also support an `overrides` attribute that defaults to `null`");
    }


    // This test validates: Citation nodes also support an `overrides` attribute that defaults to `null`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-001: Citation keyboard shortcut Mod-Shift-C dispatches scholarsynceditor-action with ', async ({ page }) => {
    // Checkpoint 1: Citation keyboard shortcut `Mod-Shift-C` dispatches `scholarsync:editor-action` with `insert-citation`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation keyboard shortcut `Mod-Shift-C` dispatches `scholarsync:editor-action` with `insert-citation`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-001.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-001 ' + "Citation keyboard shortcut `Mod-Shift-C` dispatches `scholarsync:editor-action` with `insert-citation`");
    }


    // This test validates: Citation keyboard shortcut `Mod-Shift-C` dispatches `scholarsync:editor-action` with `insert-citation`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-002: Citation chip hover waits 400 ms before opening the tooltip', async ({ page }) => {
    // Checkpoint 2: Citation chip hover waits 400 ms before opening the tooltip
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation chip hover waits 400 ms before opening the tooltip",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-002.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-002 ' + "Citation chip hover waits 400 ms before opening the tooltip");
    }


    // This test validates: Citation chip hover waits 400 ms before opening the tooltip
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-003: Citation chip click toggles the popover open state and suppresses the hover tool', async ({ page }) => {
    // Checkpoint 3: Citation chip click toggles the popover open state and suppresses the hover tooltip
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation chip click toggles the popover open state and suppresses the hover tooltip",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-003.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-003 ' + "Citation chip click toggles the popover open state and suppresses the hover tooltip");
    }


    // This test validates: Citation chip click toggles the popover open state and suppresses the hover tooltip
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-004: Citation chip fallback text is when a numeric citation has no assigned numbers', async ({ page }) => {
    // Checkpoint 4: Citation chip fallback text is `[?]` when a numeric citation has no assigned numbers
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation chip fallback text is `[?]` when a numeric citation has no assigned numbers",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-004.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-004 ' + "Citation chip fallback text is `[?]` when a numeric citation has no assigned numbers");
    }


    // This test validates: Citation chip fallback text is `[?]` when a numeric citation has no assigned numbers
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-005: Citation chip fallback text is when an author-year citation has no resolved refe', async ({ page }) => {
    // Checkpoint 5: Citation chip fallback text is `(?)` when an author-year citation has no resolved reference objects
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation chip fallback text is `(?)` when an author-year citation has no resolved reference objects",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-005.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-005 ' + "Citation chip fallback text is `(?)` when an author-year citation has no resolved reference objects");
    }


    // This test validates: Citation chip fallback text is `(?)` when an author-year citation has no resolved reference objects
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-006: Numeric citation chips compress consecutive numbers into ranges such as 1-3', async ({ page }) => {
    // Checkpoint 6: Numeric citation chips compress consecutive numbers into ranges such as `1-3`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Numeric citation chips compress consecutive numbers into ranges such as `1-3`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-006.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-006 ' + "Numeric citation chips compress consecutive numbers into ranges such as `1-3`");
    }


    // This test validates: Numeric citation chips compress consecutive numbers into ranges such as `1-3`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-007: Citation popover View action opens the reference sidebar through the Zustand sto', async ({ page }) => {
    // Checkpoint 7: Citation popover `View` action opens the reference sidebar through the Zustand store before dispatching scroll-to-reference
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation popover `View` action opens the reference sidebar through the Zustand store before dispatching scroll-to-reference",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-007.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-007 ' + "Citation popover `View` action opens the reference sidebar through the Zustand store before dispatching scroll-to-reference");
    }


    // This test validates: Citation popover `View` action opens the reference sidebar through the Zustand store before dispatching scroll-to-reference
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-008: Citation popover Remove appears only when the citation node contains more than o', async ({ page }) => {
    // Checkpoint 8: Citation popover `Remove` appears only when the citation node contains more than one `referenceId`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation popover `Remove` appears only when the citation node contains more than one `referenceId`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-008.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-008 ' + "Citation popover `Remove` appears only when the citation node contains more than one `referenceId`");
    }


    // This test validates: Citation popover `Remove` appears only when the citation node contains more than one `referenceId`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-009: Removing the final reference from a citation chip deletes the entire citation no', async ({ page }) => {
    // Checkpoint 9: Removing the final reference from a citation chip deletes the entire citation node
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Removing the final reference from a citation chip deletes the entire citation node",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-009.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-009 ' + "Removing the final reference from a citation chip deletes the entire citation node");
    }


    // This test validates: Removing the final reference from a citation chip deletes the entire citation node
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-010: Citation popover footer action label is Delete citation', async ({ page }) => {
    // Checkpoint 10: Citation popover footer action label is `Delete citation`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation popover footer action label is `Delete citation`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-010.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-010 ' + "Citation popover footer action label is `Delete citation`");
    }


    // This test validates: Citation popover footer action label is `Delete citation`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-011: Citation numbering plugin debounces renumbering by 100 ms after editor updates', async ({ page }) => {
    // Checkpoint 11: Citation numbering plugin debounces renumbering by 100 ms after editor updates
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation numbering plugin debounces renumbering by 100 ms after editor updates",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-011.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-011 ' + "Citation numbering plugin debounces renumbering by 100 ms after editor updates");
    }


    // This test validates: Citation numbering plugin debounces renumbering by 100 ms after editor updates
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-012: Citation numbering assigns numbers on order of first appearance in the document', async ({ page }) => {
    // Checkpoint 12: Citation numbering assigns numbers on order of first appearance in the document
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation numbering assigns numbers on order of first appearance in the document",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-012.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-012 ' + "Citation numbering assigns numbers on order of first appearance in the document");
    }


    // This test validates: Citation numbering assigns numbers on order of first appearance in the document
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-013: Bibliography insert command refuses to insert a second bibliography node when on', async ({ page }) => {
    // Checkpoint 13: Bibliography insert command refuses to insert a second bibliography node when one already exists
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Bibliography insert command refuses to insert a second bibliography node when one already exists",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-013.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-013 ' + "Bibliography insert command refuses to insert a second bibliography node when one already exists");
    }


    // This test validates: Bibliography insert command refuses to insert a second bibliography node when one already exists
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-014: Empty bibliography node view text reads References will appear here when you add', async ({ page }) => {
    // Checkpoint 14: Empty bibliography node view text reads `References will appear here when you add citations to your text.`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Empty bibliography node view text reads `References will appear here when you add citations to your text.`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-014.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-014 ' + "Empty bibliography node view text reads `References will appear here when you add citations to your text.`");
    }


    // This test validates: Empty bibliography node view text reads `References will appear here when you add citations to your text.`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-015: Bibliography node view heading label is References', async ({ page }) => {
    // Checkpoint 15: Bibliography node view heading label is `References`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Bibliography node view heading label is `References`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-015.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-015 ' + "Bibliography node view heading label is `References`");
    }


    // This test validates: Bibliography node view heading label is `References`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-016: Bibliography view uses pre-formatted CSL bibliography entries when bibliographyE', async ({ page }) => {
    // Checkpoint 16: Bibliography view uses pre-formatted CSL bibliography entries when `bibliographyEntries.length > 0`
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Bibliography view uses pre-formatted CSL bibliography entries when `bibliographyEntries.length > 0`",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-016.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-016 ' + "Bibliography view uses pre-formatted CSL bibliography entries when `bibliographyEntries.length > 0`");
    }


    // This test validates: Bibliography view uses pre-formatted CSL bibliography entries when `bibliographyEntries.length > 0`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-017: Bibliography view falls back to Vancouver-style text formatting when CSL bibliog', async ({ page }) => {
    // Checkpoint 17: Bibliography view falls back to Vancouver-style text formatting when CSL bibliography entries are absent
    // Section: Error Handling & Edge Cases > Footnotes, Outline Plugin, Citation Nodes, and Bibliography

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Bibliography view falls back to Vancouver-style text formatting when CSL bibliography entries are absent",
      section: "Error Handling & Edge Cases",
      subsection: "Footnotes, Outline Plugin, Citation Nodes, and Bibliography",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-017.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-017 ' + "Bibliography view falls back to Vancouver-style text formatting when CSL bibliography entries are absent");
    }


    // This test validates: Bibliography view falls back to Vancouver-style text formatting when CSL bibliography entries are absent
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-018: Citation dialog default active tab is search every time the modal opens', async ({ page }) => {
    // Checkpoint 18: Citation dialog default active tab is `search` every time the modal opens
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog default active tab is `search` every time the modal opens",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-018.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-018 ' + "Citation dialog default active tab is `search` every time the modal opens");
    }


    // This test validates: Citation dialog default active tab is `search` every time the modal opens
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-019: Citation dialog clears searchQuery selectedIds DOI state search results and sear', async ({ page }) => {
    // Checkpoint 19: Citation dialog clears `searchQuery`, `selectedIds`, DOI state, search results, and search errors on every open
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog clears `searchQuery`, `selectedIds`, DOI state, search results, and search errors on every open",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-019.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-019 ' + "Citation dialog clears `searchQuery`, `selectedIds`, DOI state, search results, and search errors on every open");
    }


    // This test validates: Citation dialog clears `searchQuery`, `selectedIds`, DOI state, search results, and search errors on every open
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-020: Citation dialog focuses the search input shortly after opening via a 50 ms timeo', async ({ page }) => {
    // Checkpoint 20: Citation dialog focuses the search input shortly after opening via a 50 ms timeout
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog focuses the search input shortly after opening via a 50 ms timeout",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-020.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-020 ' + "Citation dialog focuses the search input shortly after opening via a 50 ms timeout");
    }


    // This test validates: Citation dialog focuses the search input shortly after opening via a 50 ms timeout
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-021: Citation dialog closes on backdrop click', async ({ page }) => {
    // Checkpoint 21: Citation dialog closes on backdrop click
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog closes on backdrop click",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-021.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-021 ' + "Citation dialog closes on backdrop click");
    }


    // This test validates: Citation dialog closes on backdrop click
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-022: Citation dialog closes on Escape', async ({ page }) => {
    // Checkpoint 22: Citation dialog closes on `Escape`
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog closes on `Escape`",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-022.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-022 ' + "Citation dialog closes on `Escape`");
    }


    // This test validates: Citation dialog closes on `Escape`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-023: Citation dialog header title is Insert Citation', async ({ page }) => {
    // Checkpoint 23: Citation dialog header title is `Insert Citation`
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog header title is `Insert Citation`",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-023.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-023 ' + "Citation dialog header title is `Insert Citation`");
    }


    // This test validates: Citation dialog header title is `Insert Citation`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-024: Citation dialog tab labels are Your References Library Paste DOIPMID and Manual ', async ({ page }) => {
    // Checkpoint 24: Citation dialog tab labels are `Your References`, `Library`, `Paste DOI/PMID`, and `Manual Entry`
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog tab labels are `Your References`, `Library`, `Paste DOI/PMID`, and `Manual Entry`",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-024.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-024 ' + "Citation dialog tab labels are `Your References`, `Library`, `Paste DOI/PMID`, and `Manual Entry`");
    }


    // This test validates: Citation dialog tab labels are `Your References`, `Library`, `Paste DOI/PMID`, and `Manual Entry`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-025: Citation dialog search-input placeholder is Search references or paste DOIPMID', async ({ page }) => {
    // Checkpoint 25: Citation dialog search-input placeholder is `Search references or paste DOI/PMID...`
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Citation dialog search-input placeholder is `Search references or paste DOI/PMID...`",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-025.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-025 ' + "Citation dialog search-input placeholder is `Search references or paste DOI/PMID...`");
    }


    // This test validates: Citation dialog search-input placeholder is `Search references or paste DOI/PMID...`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-026: Search queries starting with 10 or containing doiorg are treated as DOI identifi', async ({ page }) => {
    // Checkpoint 26: Search queries starting with `10.` or containing `doi.org/` are treated as DOI identifiers
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search queries starting with `10.` or containing `doi.org/` are treated as DOI identifiers",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-026.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-026 ' + "Search queries starting with `10.` or containing `doi.org/` are treated as DOI identifiers");
    }


    // This test validates: Search queries starting with `10.` or containing `doi.org/` are treated as DOI identifiers
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-027: Search queries that are 1 to 8 digits only are treated as PMID identifiers', async ({ page }) => {
    // Checkpoint 27: Search queries that are 1 to 8 digits only are treated as PMID identifiers
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search queries that are 1 to 8 digits only are treated as PMID identifiers",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-027.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-027 ' + "Search queries that are 1 to 8 digits only are treated as PMID identifiers");
    }


    // This test validates: Search queries that are 1 to 8 digits only are treated as PMID identifiers
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-028: Search-tab non-identifier PubMed search waits 350 ms after typing before request', async ({ page }) => {
    // Checkpoint 28: Search-tab non-identifier PubMed search waits 350 ms after typing before requesting `/api/references/search-pubmed`
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search-tab non-identifier PubMed search waits 350 ms after typing before requesting `/api/references/search-pubmed`",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-028.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-028 ' + "Search-tab non-identifier PubMed search waits 350 ms after typing before requesting `/api/references/search-pubmed`");
    }


    // This test validates: Search-tab non-identifier PubMed search waits 350 ms after typing before requesting `/api/references/search-pubmed`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-029: Search-tab PubMed request body includes query page 1 pageSize 10 and documentId', async ({ page }) => {
    // Checkpoint 29: Search-tab PubMed request body includes `query`, `page: 1`, `pageSize: 10`, and `documentId`
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search-tab PubMed request body includes `query`, `page: 1`, `pageSize: 10`, and `documentId`",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-029.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-029 ' + "Search-tab PubMed request body includes `query`, `page: 1`, `pageSize: 10`, and `documentId`");
    }


    // This test validates: Search-tab PubMed request body includes `query`, `page: 1`, `pageSize: 10`, and `documentId`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-030: Search-tab search spinner shows while the PubMed request is pending', async ({ page }) => {
    // Checkpoint 30: Search-tab search spinner shows while the PubMed request is pending
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search-tab search spinner shows while the PubMed request is pending",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-030.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-030 ' + "Search-tab search spinner shows while the PubMed request is pending");
    }


    // This test validates: Search-tab search spinner shows while the PubMed request is pending
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-031: Search-tab error fallback string is PubMed search failed Please try again', async ({ page }) => {
    // Checkpoint 31: Search-tab error fallback string is `PubMed search failed. Please try again.`
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search-tab error fallback string is `PubMed search failed. Please try again.`",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-031.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-031 ' + "Search-tab error fallback string is `PubMed search failed. Please try again.`");
    }


    // This test validates: Search-tab error fallback string is `PubMed search failed. Please try again.`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-032: Search-tab empty state text is No PubMed or reference matches found when a typed', async ({ page }) => {
    // Checkpoint 32: Search-tab empty state text is `No PubMed or reference matches found.` when a typed non-identifier query returns nothing
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search-tab empty state text is `No PubMed or reference matches found.` when a typed non-identifier query returns nothing",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-032.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-032 ' + "Search-tab empty state text is `No PubMed or reference matches found.` when a typed non-identifier query returns nothing");
    }


    // This test validates: Search-tab empty state text is `No PubMed or reference matches found.` when a typed non-identifier query returns nothing
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-033: Search-tab empty state text is No references yet Add one using DOIPMID or manual', async ({ page }) => {
    // Checkpoint 33: Search-tab empty state text is `No references yet. Add one using DOI/PMID or manual entry.` when there are no saved references and no active text query
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search-tab empty state text is `No references yet. Add one using DOI/PMID or manual entry.` when there are no saved references and no active text query",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-033.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-033 ' + "Search-tab empty state text is `No references yet. Add one using DOI/PMID or manual entry.` when there are no saved references and no active text query");
    }


    // This test validates: Search-tab empty state text is `No references yet. Add one using DOI/PMID or manual entry.` when there are no saved references and no active text query
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-034: Search-tab empty state text is No matching references found when local reference', async ({ page }) => {
    // Checkpoint 34: Search-tab empty state text is `No matching references found.` when local references exist but filtering returns nothing
    // Section: Error Handling & Edge Cases > Citation Dialog: Shared Modal and Search Tab

    // Navigate to the page
    await page.goto('/studio', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-022');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Search-tab empty state text is `No matching references found.` when local references exist but filtering returns nothing",
      section: "Error Handling & Edge Cases",
      subsection: "Citation Dialog: Shared Modal and Search Tab",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-034.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-034 ' + "Search-tab empty state text is `No matching references found.` when local references exist but filtering returns nothing");
    }


    // This test validates: Search-tab empty state text is `No matching references found.` when local references exist but filtering returns nothing
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });
});
