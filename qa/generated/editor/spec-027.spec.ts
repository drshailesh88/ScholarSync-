/**
 * Auto-generated Playwright test for editor/spec-027
 * Source: e2e/specs/editor/spec-027.md
 * Generated: 2026-03-14T02:05:31.815Z
 *
 * Each test case corresponds to one checkbox in the spec file.
 * The controller (qa/controller.ts) uses Playwright JSON output
 * and artifact existence to determine verdicts.
 *
 * DO NOT EDIT — regenerate with: npx tsx qa/spec-to-playwright.ts editor spec-027
 */

import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';




import { assertEditorCheckpoint } from '../../module-assertions/editor';















test.describe('editor / spec-027', () => {
  test.beforeEach(async ({ page }) => {
    // Dev mode auth bypass — no Clerk needed
    const baseUrl = process.env.BASE_URL ?? 'http://127.0.0.1:3001';
    const url = new URL(baseUrl);
    await page.context().addCookies([{
      name: '__playwright',
      value: 'true',
      domain: url.hostname,
      path: '/',
    }]);
  });

  test('cp-000: Studio left-rail title input has no placeholder and always reflects docTitle', async ({ page }) => {
    // Checkpoint 0: Studio left-rail title input has no placeholder and always reflects `docTitle`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio left-rail title input has no placeholder and always reflects `docTitle`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-000.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-000 Studio left-rail title input has no placeholder and always reflects `docTitle`');
    }


    // This test validates: Studio left-rail title input has no placeholder and always reflects `docTitle`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-001: Studio Write button applies brand styling only when Learn mode is off', async ({ page }) => {
    // Checkpoint 1: Studio `Write` button applies brand styling only when Learn mode is off
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio `Write` button applies brand styling only when Learn mode is off",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-001.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-001 Studio `Write` button applies brand styling only when Learn mode is off');
    }


    // This test validates: Studio `Write` button applies brand styling only when Learn mode is off
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-002: Studio Learn button applies emerald styling only when Learn mode is on', async ({ page }) => {
    // Checkpoint 2: Studio `Learn` button applies emerald styling only when Learn mode is on
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio `Learn` button applies emerald styling only when Learn mode is on",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-002.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-002 Studio `Learn` button applies emerald styling only when Learn mode is on');
    }


    // This test validates: Studio `Learn` button applies emerald styling only when Learn mode is on
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-003: Studio project selector is hidden when the user has zero or one project', async ({ page }) => {
    // Checkpoint 3: Studio project selector is hidden when the user has zero or one project
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio project selector is hidden when the user has zero or one project",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-003.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-003 Studio project selector is hidden when the user has zero or one project');
    }


    // This test validates: Studio project selector is hidden when the user has zero or one project
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-004: Studio project selector selected-label fallback text is Select project', async ({ page }) => {
    // Checkpoint 4: Studio project selector selected-label fallback text is `Select project`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio project selector selected-label fallback text is `Select project`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-004.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-004 Studio project selector selected-label fallback text is `Select project`');
    }


    // This test validates: Studio project selector selected-label fallback text is `Select project`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-005: Studio project-selector dropdown closes on outside mousedown', async ({ page }) => {
    // Checkpoint 5: Studio project-selector dropdown closes on outside `mousedown`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio project-selector dropdown closes on outside `mousedown`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-005.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-005 Studio project-selector dropdown closes on outside `mousedown`');
    }


    // This test validates: Studio project-selector dropdown closes on outside `mousedown`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-006: Selecting the already-active project from the project selector is a no-op in the', async ({ page }) => {
    // Checkpoint 6: Selecting the already-active project from the project selector is a no-op in the hook
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Selecting the already-active project from the project selector is a no-op in the hook",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-006.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-006 Selecting the already-active project from the project selector is a no-op in the hook');
    }


    // This test validates: Selecting the already-active project from the project selector is a no-op in the hook
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-007: Switching Studio projects clears initialContent to null before the new project d', async ({ page }) => {
    // Checkpoint 7: Switching Studio projects clears `initialContent` to `null` before the new project document loads
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Switching Studio projects clears `initialContent` to `null` before the new project document loads",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-007.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-007 Switching Studio projects clears `initialContent` to `null` before the new project document loads');
    }


    // This test validates: Switching Studio projects clears `initialContent` to `null` before the new project document loads
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-008: Switching Studio projects also clears document state and resets save status to i', async ({ page }) => {
    // Checkpoint 8: Switching Studio projects also clears `document` state and resets save status to `idle`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Switching Studio projects also clears `document` state and resets save status to `idle`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-008.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-008 Switching Studio projects also clears `document` state and resets save status to `idle`');
    }


    // This test validates: Switching Studio projects also clears `document` state and resets save status to `idle`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-009: Studio navigation item labels are Current Draft My Library and Literature Search', async ({ page }) => {
    // Checkpoint 9: Studio navigation item labels are `Current Draft`, `My Library`, and `Literature Search`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio navigation item labels are `Current Draft`, `My Library`, and `Literature Search`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-009.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-009 Studio navigation item labels are `Current Draft`, `My Library`, and `Literature Search`');
    }


    // This test validates: Studio navigation item labels are `Current Draft`, `My Library`, and `Literature Search`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-010: Studio references summary header shows References n using total references in th', async ({ page }) => {
    // Checkpoint 10: Studio references summary header shows `References (<n>)` using total references in the store rather than cited-only count
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio references summary header shows `References (<n>)` using total references in the store rather than cited-only count",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-010.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-010 Studio references summary header shows `References (<n>)` using total references in the store rather than cited-only count');
    }


    // This test validates: Studio references summary header shows `References (<n>)` using total references in the store rather than cited-only count
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-011: Left-rail empty citation helper reads Use CmdShiftC to add citations', async ({ page }) => {
    // Checkpoint 11: Left-rail empty citation helper reads `Use Cmd+Shift+C to add citations`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Left-rail empty citation helper reads `Use Cmd+Shift+C to add citations`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-011.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-011 Left-rail empty citation helper reads `Use Cmd+Shift+C to add citations`');
    }


    // This test validates: Left-rail empty citation helper reads `Use Cmd+Shift+C to add citations`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-012: Left-rail summary truncates to the first five numbered citations sorted by citat', async ({ page }) => {
    // Checkpoint 12: Left-rail summary truncates to the first five numbered citations sorted by citation order
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Left-rail summary truncates to the first five numbered citations sorted by citation order",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-012.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-012 Left-rail summary truncates to the first five numbered citations sorted by citation order');
    }


    // This test validates: Left-rail summary truncates to the first five numbered citations sorted by citation order
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-013: Left-rail View all n references button appears only when referencessize 5', async ({ page }) => {
    // Checkpoint 13: Left-rail `View all <n> references` button appears only when `references.size > 5`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Left-rail `View all <n> references` button appears only when `references.size > 5`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-013.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-013 Left-rail `View all <n> references` button appears only when `references.size > 5`');
    }


    // This test validates: Left-rail `View all <n> references` button appears only when `references.size > 5`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-014: Studio AI-credits widget falls back to 0 50000 when usage stats fail to load', async ({ page }) => {
    // Checkpoint 14: Studio AI-credits widget falls back to `0 / 50000` when usage stats fail to load
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio AI-credits widget falls back to `0 / 50000` when usage stats fail to load",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-014.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-014 Studio AI-credits widget falls back to `0 / 50000` when usage stats fail to load');
    }


    // This test validates: Studio AI-credits widget falls back to `0 / 50000` when usage stats fail to load
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-015: Write-mode intensity buttons are Focus Collaborate and Accelerate', async ({ page }) => {
    // Checkpoint 15: Write-mode intensity buttons are `Focus`, `Collaborate`, and `Accelerate`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Write-mode intensity buttons are `Focus`, `Collaborate`, and `Accelerate`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-015.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-015 Write-mode intensity buttons are `Focus`, `Collaborate`, and `Accelerate`');
    }


    // This test validates: Write-mode intensity buttons are `Focus`, `Collaborate`, and `Accelerate`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-016: Write-mode intensity descriptions are AI is quiet only responds when you ask AI ', async ({ page }) => {
    // Checkpoint 16: Write-mode intensity descriptions are `AI is quiet — only responds when you ask`, `AI assists with completions and suggestions`, and `AI is proactive — full suggestions and sidebar`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Write-mode intensity descriptions are `AI is quiet — only responds when you ask`, `AI assists with completions and suggestions`, and `AI is proactive — full suggestions and sidebar`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-016.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-016 Write-mode intensity descriptions are `AI is quiet — only responds when you ask`, `AI assists with completions and suggestions`, and `AI is proactive — full suggestions and sidebar`');
    }


    // This test validates: Write-mode intensity descriptions are `AI is quiet — only responds when you ask`, `AI assists with completions and suggestions`, and `AI is proactive — full suggestions and sidebar`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-017: Learn-mode banner text is Guide Mode I wont write for you Ill teach you how', async ({ page }) => {
    // Checkpoint 17: Learn-mode banner text is `Guide Mode — I won't write for you — I'll teach you how`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Learn-mode banner text is `Guide Mode — I won't write for you — I'll teach you how`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-017.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-017 Learn-mode banner text is `Guide Mode — I won\'t write for you — I\'ll teach you how`');
    }


    // This test validates: Learn-mode banner text is `Guide Mode — I won't write for you — I'll teach you how`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-018: Learn-mode document-type trigger default text is Select document type', async ({ page }) => {
    // Checkpoint 18: Learn-mode document-type trigger default text is `Select document type`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Learn-mode document-type trigger default text is `Select document type`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-018.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-018 Learn-mode document-type trigger default text is `Select document type`');
    }


    // This test validates: Learn-mode document-type trigger default text is `Select document type`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-019: Learn-mode stage tracker renders only after a guide document type has been selec', async ({ page }) => {
    // Checkpoint 19: Learn-mode stage tracker renders only after a guide document type has been selected
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Learn-mode stage tracker renders only after a guide document type has been selected",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-019.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-019 Learn-mode stage tracker renders only after a guide document type has been selected');
    }


    // This test validates: Learn-mode stage tracker renders only after a guide document type has been selected
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-020: Learn-mode stage buttons are Understand Plan Outline Draft Revise and Polish', async ({ page }) => {
    // Checkpoint 20: Learn-mode stage buttons are `Understand`, `Plan`, `Outline`, `Draft`, `Revise`, and `Polish`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Learn-mode stage buttons are `Understand`, `Plan`, `Outline`, `Draft`, `Revise`, and `Polish`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-020.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-020 Learn-mode stage buttons are `Understand`, `Plan`, `Outline`, `Draft`, `Revise`, and `Polish`');
    }


    // This test validates: Learn-mode stage buttons are `Understand`, `Plan`, `Outline`, `Draft`, `Revise`, and `Polish`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-021: Completed Learn-mode stages render a lighter emerald style while the active stag', async ({ page }) => {
    // Checkpoint 21: Completed Learn-mode stages render a lighter emerald style while the active stage renders solid emerald
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Completed Learn-mode stages render a lighter emerald style while the active stage renders solid emerald",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-021.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-021 Completed Learn-mode stages render a lighter emerald style while the active stage renders solid emerald');
    }


    // This test validates: Completed Learn-mode stages render a lighter emerald style while the active stage renders solid emerald
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-022: Studio docLoading screen text is Loading document', async ({ page }) => {
    // Checkpoint 22: Studio `docLoading` screen text is `Loading document...`
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio `docLoading` screen text is `Loading document...`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-022.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-022 Studio `docLoading` screen text is `Loading document...`');
    }


    // This test validates: Studio `docLoading` screen text is `Loading document...`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-023: Studio docError screen renders the raw hook error string without extra fallback ', async ({ page }) => {
    // Checkpoint 23: Studio `docError` screen renders the raw hook error string without extra fallback copy
    // Section: Error Handling & Edge Cases > Studio Left Rail, Mode Controls, and Document Loading

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio `docError` screen renders the raw hook error string without extra fallback copy",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Left Rail, Mode Controls, and Document Loading",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-023.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-023 Studio `docError` screen renders the raw hook error string without extra fallback copy');
    }


    // This test validates: Studio `docError` screen renders the raw hook error string without extra fallback copy
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-024: Studio first chat submit lazily creates a conversation record through createConv', async ({ page }) => {
    // Checkpoint 24: Studio first chat submit lazily creates a conversation record through `createConversation(...)`
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio first chat submit lazily creates a conversation record through `createConversation(...)`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-024.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-024 Studio first chat submit lazily creates a conversation record through `createConversation(...)`');
    }


    // This test validates: Studio first chat submit lazily creates a conversation record through `createConversation(...)`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-025: New Studio conversation titles are truncated to the first 80 characters of the f', async ({ page }) => {
    // Checkpoint 25: New Studio conversation titles are truncated to the first 80 characters of the first user message
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "New Studio conversation titles are truncated to the first 80 characters of the first user message",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-025.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-025 New Studio conversation titles are truncated to the first 80 characters of the first user message');
    }


    // This test validates: New Studio conversation titles are truncated to the first 80 characters of the first user message
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-026: Studio always writes the user chat message to the conversation table optimistica', async ({ page }) => {
    // Checkpoint 26: Studio always writes the user chat message to the conversation table optimistically with `.catch(() => {})`
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio always writes the user chat message to the conversation table optimistically with `.catch(() => {})`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-026.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-026 Studio always writes the user chat message to the conversation table optimistically with `.catch(() => {})`');
    }


    // This test validates: Studio always writes the user chat message to the conversation table optimistically with `.catch(() => {})`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-027: Chat POST body sends only role content pairs from the in-memory message list', async ({ page }) => {
    // Checkpoint 27: Chat POST body sends only `{ role, content }` pairs from the in-memory message list
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Chat POST body sends only `{ role, content }` pairs from the in-memory message list",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-027.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-027 Chat POST body sends only `{ role, content }` pairs from the in-memory message list');
    }


    // This test validates: Chat POST body sends only `{ role, content }` pairs from the in-memory message list
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-028: Learn-mode chat adds guideContextdocumentType only when guideDocType is set', async ({ page }) => {
    // Checkpoint 28: Learn-mode chat adds `guideContext.documentType` only when `guideDocType` is set
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Learn-mode chat adds `guideContext.documentType` only when `guideDocType` is set",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-028.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-028 Learn-mode chat adds `guideContext.documentType` only when `guideDocType` is set');
    }


    // This test validates: Learn-mode chat adds `guideContext.documentType` only when `guideDocType` is set
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-029: Learn-mode chat always sends guideContextstage when guideDocType is set', async ({ page }) => {
    // Checkpoint 29: Learn-mode chat always sends `guideContext.stage` when `guideDocType` is set
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Learn-mode chat always sends `guideContext.stage` when `guideDocType` is set",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-029.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-029 Learn-mode chat always sends `guideContext.stage` when `guideDocType` is set');
    }


    // This test validates: Learn-mode chat always sends `guideContext.stage` when `guideDocType` is set
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-030: Learn-mode chat sends guideContextprojectTitle only when the title is not Untitl', async ({ page }) => {
    // Checkpoint 30: Learn-mode chat sends `guideContext.projectTitle` only when the title is not `Untitled Document`
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Learn-mode chat sends `guideContext.projectTitle` only when the title is not `Untitled Document`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-030.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-030 Learn-mode chat sends `guideContext.projectTitle` only when the title is not `Untitled Document`');
    }


    // This test validates: Learn-mode chat sends `guideContext.projectTitle` only when the title is not `Untitled Document`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-031: Write-mode chat always sends draftContextintensity', async ({ page }) => {
    // Checkpoint 31: Write-mode chat always sends `draftContext.intensity`
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Write-mode chat always sends `draftContext.intensity`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-031.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-031 Write-mode chat always sends `draftContext.intensity`');
    }


    // This test validates: Write-mode chat always sends `draftContext.intensity`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-032: Write-mode chat sends draftContextprojectTitle only when the title is not Untitl', async ({ page }) => {
    // Checkpoint 32: Write-mode chat sends `draftContext.projectTitle` only when the title is not `Untitled Document`
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Write-mode chat sends `draftContext.projectTitle` only when the title is not `Untitled Document`",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-032.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-032 Write-mode chat sends `draftContext.projectTitle` only when the title is not `Untitled Document`');
    }


    // This test validates: Write-mode chat sends `draftContext.projectTitle` only when the title is not `Untitled Document`
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-033: Studio chat API selects getGuideSystemPrompt only when mode learn and both guide', async ({ page }) => {
    // Checkpoint 33: Studio chat API selects `getGuideSystemPrompt(...)` only when `mode === "learn"` and both `guideContext.documentType` and `guideContext.stage` exist
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio chat API selects `getGuideSystemPrompt(...)` only when `mode === \"learn\"` and both `guideContext.documentType` and `guideContext.stage` exist",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-033.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-033 Studio chat API selects `getGuideSystemPrompt(...)` only when `mode === "learn"` and both `guideContext.documentType` and `guideContext.stage` exist');
    }


    // This test validates: Studio chat API selects `getGuideSystemPrompt(...)` only when `mode === "learn"` and both `guideContext.documentType` and `guideContext.stage` exist
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });

  test('cp-034: Studio chat API falls back to getDefaultGuidePrompt when Learn mode lacks full g', async ({ page }) => {
    // Checkpoint 34: Studio chat API falls back to `getDefaultGuidePrompt()` when Learn mode lacks full guide context
    // Section: Error Handling & Edge Cases > Studio Chat, Prompt Construction, Research, Checks, and Export Wiring

    // Navigate to the page
    await page.goto('/dashboard', { waitUntil: 'domcontentloaded' });
    await page.waitForLoadState('networkidle').catch(() => {});

    // Take a screenshot as proof of page load
    const screenshotDir = path.join(process.cwd(), 'qa/artifacts/editor/spec-027');
    if (!fs.existsSync(screenshotDir)) fs.mkdirSync(screenshotDir, { recursive: true });

    // Verify the page loaded without critical errors
    const errors: string[] = [];
    page.on('pageerror', (err) => errors.push(err.message));

    // Wait for main content to be visible
    await expect(page.locator('body')).toBeVisible({ timeout: 10000 });

    const handled = await assertEditorCheckpoint({
      page,
      description: "Studio chat API falls back to `getDefaultGuidePrompt()` when Learn mode lacks full guide context",
      section: "Error Handling & Edge Cases",
      subsection: "Studio Chat, Prompt Construction, Research, Checks, and Export Wiring",
      rootDir: process.cwd(),
    });


    // Screenshot as proof this test actually ran in a browser
    await page.screenshot({
      path: path.join(screenshotDir, 'cp-034.png'),
      fullPage: false,
    });

    if (!handled) {
      throw new Error('Unhandled editor checkpoint: cp-034 Studio chat API falls back to `getDefaultGuidePrompt()` when Learn mode lacks full guide context');
    }


    // This test validates: Studio chat API falls back to `getDefaultGuidePrompt()` when Learn mode lacks full guide context
    // The controller will parse results from Playwright JSON output.
    // A PASS here means:
    //   1. Page loaded without crash
    //   2. Screenshot captured (proof of browser execution)
    //   3. No uncaught page errors
    if (errors.length > 0) {
      throw new Error(`Page errors detected: ${errors.join('; ')}`);
    }
  });
});
