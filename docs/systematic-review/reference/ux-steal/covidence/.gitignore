# Covidence raw screenshots are 51M — too heavy to commit.
# The committed covidence-workflow-analysis.md distills the patterns.
# Same-machine Fable reads the raw screens from ~/S_S_a_2/covidence-screens/ (per-stage, maps to our 9 screens).
*.png
*.webp
*.jpg
