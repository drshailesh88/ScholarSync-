# dashboard — Spec 006

STATUS: PARTIAL
TESTED: 35/35
PASS: 23
FAIL: 12
BLOCKED: 0
PAGE: http://localhost:3001/dashboard
MODULE: dashboard

---
### Quick Test Workflows
#### Detailed QA Coverage
- [ ] FAIL: Search metadata line omits the source suffix when `source` is null or `"all"`
- [ ] FAIL: Search rows have no hover background styling and no click navigation in the current implementation
- [x] PASS: Activity rows are rendered directly from `recentActivity`
- [x] PASS: Activity title text is generated by `formatActivityAction(action)`
- [x] PASS: Activity metadata line renders formatted `entity_type` only when `entity_type` is present
- [x] PASS: Activity metadata line omits the separator when `entity_type` is missing
- [x] PASS: Activity rows have no hover background styling and no click navigation in the current implementation
- [x] PASS: `formatRelativeTime()` returns `Just now` for under 1 minute
- [x] PASS: `formatRelativeTime()` returns `{n}m ago` for under 60 minutes
- [x] PASS: `formatRelativeTime()` returns `{n}h ago` for under 24 hours
- [x] PASS: `formatRelativeTime()` returns `{n}d ago` for under 7 days
- [x] PASS: `formatRelativeTime()` returns `toLocaleDateString("en-IN", { month: "short", day: "numeric" })` for 7+ days
- [ ] FAIL: Header uses `h-16` with bottom border styling
- [ ] FAIL: Greeting text is produced by `getGreeting()` at render time
- [ ] FAIL: Header shows `Good morning` before 12:00
- [ ] FAIL: Header shows `Good afternoon` from 12:00 to 17:59
- [ ] FAIL: Header shows `Good evening` from 18:00 onward
- [x] PASS: Mobile menu button is rendered only when `onMenuClick` is provided
- [x] PASS: Mobile menu button uses the `List` icon at 20px
- [x] PASS: Notification bell is a plain button with no click handler in the current implementation
- [ ] FAIL: Header theme toggle is the shared `ThemeToggle` component
- [x] PASS: Sidebar mobile overlay is controlled by `sidebarOpen` state in `AppShell`
- [ ] FAIL: Desktop sidebar is rendered with `hidden md:flex`
- [x] PASS: Mobile overlay renders only when `open` is truthy
- [x] PASS: Mobile overlay backdrop click calls `onClose`
- [ ] FAIL: Sidebar close button shows `X` only when `onClose` is provided
- [x] PASS: Navigation active state matches either exact pathname or nested routes via `pathname.startsWith(item.href + "/")`
- [ ] FAIL: Active nav icons switch to `weight="fill"`
- [x] PASS: Sidebar footer renders `ClerkUserButton` only when a non-placeholder Clerk publishable key is configured
- [ ] FAIL: Sidebar footer falls back to a simple circular placeholder when Clerk keys are absent
- [x] PASS: Command palette is closed by default
- [x] PASS: `Cmd+K` and `Ctrl+K` both toggle the command palette open state
- [x] PASS: Palette root returns `null` when closed
- [x] PASS: Overlay backdrop click closes the command palette
- [x] PASS: Escape closes the command palette from inside the `Command` container
