# compliance — Spec 013

STATUS: DONE
TESTED: 35/35
PASS: 35
FAIL: 0
BLOCKED: 0
PAGE: http://localhost:3001/compliance
MODULE: compliance

---
### Quick Test Workflows
#### API Route — `/api/integrity-check` (main route, additional details)
- [x] PASS: `flaggedPassages` DB filter uses `humanProbability < 50` threshold (different from IntegrityPanel's `< 40` and the Humanize button's `< 40`)
- [x] PASS: `aiDetectionDetails` persisted as object with keys `humanScore`, `overallRisk`, `engine`, `stats`, and `paragraphCount` (count, not the full paragraphs array)
- [x] PASS: `aiDetectionEngine` stored as a separate DB column in addition to `aiDetectionDetails.engine`
- [x] PASS: Exact error when AI engine throws at runtime: `"AI detection service is unavailable. Please try again later."` (status 503)
- [x] PASS: Exact error when `isAIConfigured()` returns false: `"AI service is not configured."` (status 503)
- [x] PASS: `sourceMatches` DB field duplicates `result.plagiarism?.matches ?? null` alongside `plagiarismMatches`
#### API Route — `/api/integrity-check/report` (detailed structure)
- [x] PASS: Report Zod validation for `stats` only validates 6 of 10 `TextStatistics` fields: `avgSentenceLength`, `sentenceLengthStdDev`, `typeTokenRatio`, `passiveVoicePercent`, `readabilityGrade`, `hedgingPhraseCount` — the 4 newer fields (`formulaicTransitionDensity`, `paragraphLengthStdDev`, `repetitiveSentenceOpeningRatio`, `markdownHeadingCount`) are not validated
- [x] PASS: Report date formatted with `en-US` locale using `{ year: "numeric", month: "long", day: "numeric" }`
- [x] PASS: Report `documentTitle` defaults to `"Untitled"` server-side when the optional field is absent
- [x] PASS: Report tier displayed as `"Premium"` for `tier === "paid"`, `"Free"` for `tier === "free"`
- [x] PASS: Report footer exact text: `*Generated by ScholarSync Integrity Check on {date}.*`
- [x] PASS: Report plagiarism match table columns: `Excerpt | Source | Year | Similarity | Severity | DOI`
- [x] PASS: Report AI paragraph table columns: `# | Excerpt | Human Prob. | Flags | Suggestion`
- [x] PASS: Report table sanitizes AI/plagiarism excerpts and suggestions with `.replace(/\|/g, "\\|").replace(/\n/g, " ")`; flags and source titles escape pipe characters only
- [x] PASS: Report plagiarism no-matches text: `"No plagiarism matches found."`
- [x] PASS: Report citation no-issues text: `"No citation issues found."`
- [x] PASS: Report citation issues formatted as: `- **[{SEVERITY}]** {message}{ (ref: {reference})}`
- [x] PASS: Report DOI links use markdown format: `[{doi}](https://doi.org/{doi})`
- [x] PASS: Report plagiarism similarity formatted as `Math.round(m.similarity * 100)%`, severity as `.toUpperCase()`
#### API Route — `/api/integrity-check/paraphrase` (additional details)
- [x] PASS: Paraphrase Zod schema also accepts optional `sourceYear` as `z.number().int().positive()` (not sent by the page client, but accepted by the API)
- [x] PASS: Paraphrase builds source info by joining filtered non-null parts: `Title: "{sourceTitle}"`, optional `DOI: {sourceDoi}`, optional `Year: {sourceYear}` — comma-space separated
- [x] PASS: Paraphrase uses `generateObject` from Vercel AI SDK with a structured `paraphraseResponseSchema`
- [x] PASS: Paraphrase traces generation with `{ tier: "small", modelId: "claude-haiku-4-5-20251001", feature: "integrity-paraphrase" }`
- [x] PASS: Paraphrase exact system prompt: `"Rewrite this passage to express the same ideas in original language. Add a proper citation to the source. The goal is to reduce plagiarism similarity while maintaining academic integrity. Output both the paraphrased text and a suggested citation string."`
- [x] PASS: Paraphrase returns 503 with `"AI service is not configured."` if AI not configured
- [x] PASS: Paraphrase 500 error body: `{ error: "Failed to paraphrase text" }`
#### API Route — `/api/integrity-check/humanize` (additional details)
- [x] PASS: Humanize Zod schema accepts optional `context` field: `z.string().max(2000, "Context must not exceed 2000 characters")`
- [x] PASS: When `context` is provided, it is appended to the user prompt as `\n\nSurrounding context for tone matching:\n{context}`
- [x] PASS: Humanize uses `generateObject` from Vercel AI SDK with a structured `humanizeResponseSchema`
- [x] PASS: Humanize traces generation with `{ tier: "small", modelId: "claude-haiku-4-5-20251001", feature: "integrity-humanize" }`
- [x] PASS: Humanize exact system prompt: `"Rewrite this academic paragraph to sound more naturally human-written. Vary sentence lengths, reduce hedging phrases, use active voice where appropriate, and maintain academic rigor. Do NOT change the meaning or remove citations."`
- [x] PASS: Humanize returns 503 with `"AI service is not configured."` if AI not configured
- [x] PASS: Humanize 500 error body: `{ error: "Failed to humanize text" }`
#### API Route — `/api/copyleaks` (additional details)
- [x] PASS: Copyleaks exact error for missing credentials: `"Plagiarism checking service is not configured. Please contact your administrator."` (checks `COPYLEAKS_EMAIL` and `COPYLEAKS_API_KEY` env vars)
- [x] PASS: Copyleaks `scan` action without `text` returns 400 with `"Text is required for scan action"`
